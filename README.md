# 小麦 — 一个住在 Minecraft 服务器里的 AI 玩家

> **Field notes from running an LLM-driven [mineflayer](https://github.com/PrismarineJS/mineflayer) bot 24/7
> on a real, populated Minecraft server** — driven by a *small local model* (qwen3:8b), not a frontier API.
> Most of the value here is in [`docs/`](docs/): quantified failure modes, with the measurements that found them.
> Written in Chinese; the numbers and code are language-independent.

小麦(XiaoMai)是一个 **完全由 AI 操作、以真人玩家身份登录** 的 Minecraft 机器人。
它不是站桩 NPC —— 它自己走路、砍树、合成、打怪、安家、被怪打死、再爬起来。
每一步做什么由一个 **跑在家里局域网的小模型** 决定,不是云端大模型。

它住在一个**真实有人玩的服务器**上(一群小孩在同一个世界里盖房子),所以
"不破坏别人的东西" 是硬性红线,不是加分项。

---

## 这个仓库为什么值得看

不是因为代码写得好,是因为 **它跑了很久,而且每个结论后面都有实测数字**。

mineflayer 的文档告诉你 API 怎么调;这个仓库记录的是 **API 调对了、机器人还是不干活** 的那些情况。

几条有代表性的:

| 现象 | 看起来的原因 | 实测出来的真因 |
|---|---|---|
| 砍树成功率只有 **14%**(178 次尝试 25 次拿到木头) | 附近没树 / 树被判成建筑 | **背包 36 格全满**,`collectblock` 在挖之前就抛 `There are no defined chest locations!` —— 每一次都必然失败,和有没有树无关 |
| 合成"跑完了但背包里没多出东西" | 时序问题,等的时间不够 | 同一个原因:**背包满了,合成出来的东西没地方放** |
| 它老说"附近有树但砍不了" | 护栏太严 | 护栏没接到 `collectblock` **自己新建的那份 `Movements`** 上(它会顶掉你的) |
| 一次死亡,日志里刷出 5 次 | 死亡循环 / 跑了两个进程(**两个猜测都被自己证伪**) | 传送(EssentialsX `/home`)会让服务端下发重生包,`bot.on('death')` 被重复触发 |
| 修了弓箭手(自杀式冲锋 1174 → 0 次)之后,**总死亡率反而升高** | —— | 修 A 打破了 B:新加的"自己造装备"链条抢占了"吃饭"的槽位,饿死从 0.8 → 2.7 次/小时 |

最后一条是这个项目最贵的一课:**局部修复会在别处制造新病灶,只看单项指标会骗自己。**

---

## 架构

```
      ┌──────────────┐   每 2.5 秒一次,给一段「处境」+ 固定动作菜单
      │  本地小模型   │ ←──────────────────────────────────────┐
      │  qwen3:8b    │   返回严格 JSON:{"action":"gather_wood"}│
      └──────────────┘                                        │
                                                              │
   ┌──────────────────────────────────────────────────────────┴────┐
   │  bot.real.js                                                  │
   │                                                               │
   │   反射层 reflexTick  每 1 秒 ── 保命、逃跑、吃饭、腾背包        │
   │        │                       (不问大脑,直接夺权)           │
   │        ↓                                                      │
   │   硬规则 forced     ── "有木头没箱子 → 回家造箱子"(不问大脑)  │
   │        ↓                                                      │
   │   大脑决策          ── 从菜单里挑一个 + 参数                   │
   │        ↓                                                      │
   │   执行器 executeAction ── 30 秒封顶,失败要如实说原因          │
   └───────────────────────────────────────────────────────────────┘
```

**三层的分工是这个项目最重要的设计决定:**

- **能力弱的模型不该决定生死。** 血量见底要不要逃跑、背包满了要不要腾格子,
  这些交给反射层,每秒判一次,不进菜单。
- **"必须先做 A 才能做 B" 靠叮嘱不管用。** 实测三次验证:在提示词里写
  "你要先安家再建造",小模型不会照做。只能靠 **菜单结构**(做不到的动作根本不出现)
  和 **硬规则**(直接夺权)。
- **模型负责"想做什么",代码负责"做得成不成"。** 例如玩家要东西时,
  给不给、给多少由模型按人格决定;而"背包里没有就给不出""不能超过持有量"
  "同一个人 3 分钟只能要一次" 由代码兜住。

## 目录

```
src/
  bot.js         入口壳子(17 行)—— 靠标记文件切换 正常 / 安全 / 探测 三种模式
  bot.real.js    机器人本体(4000+ 行,注释密度很高,大量实测记录写在注释里)
  brain.js       和模型对话:提示词、JSON 校验、超时降级
  roles.js       角色机制:一份代码跑多个 AI 角色(父子/夫妻),各有人格和动作白名单
  roles.json     角色定义(人格文本可热更新,不用重启)
  places.js      地点记忆:树在哪、动物在哪、在哪死过
deploy_safe.sh   带闸门的部署脚本:语法检查 → 关键字闸门 → 备份 → 替换 → 观察 300 秒出报表
docs/            ← 真正的干货
```

## 跑起来

```bash
npm install
cp .env.example .env    # 填你自己的服务器和模型地址
node src/bot.js
```

需要一个 Ollama 兼容的 `/api/chat` 端点。模型建议见
[`docs/03-local-small-model.md`](docs/03-local-small-model.md)——
**`qwen3` 系列必须显式传 `think: false`**,不传会返回空内容。

## 文档

- [01 · 背包满了是隐形的总开关](docs/01-inventory-full-blocks-everything.md) —— 一个静默的硬阻塞,同时废掉砍树和合成
- [02 · 实测踩过的坑](docs/02-field-pitfalls.md) —— 会反复咬人的那些,含测量方法本身的坑
- [03 · 接本地小模型的坑](docs/03-local-small-model.md) —— `think:false`、菜单设计、结构化数据必须原样透传
- [04 · 不碰别人东西的护栏](docs/04-guardrails.md) —— 在有真人玩的服务器上跑机器人的红线
- [05 · "家" 有两份](docs/05-two-sources-of-truth.md) —— 服务端权威状态 vs 机器人自己记的,以及对不上时的死锁
- [06 · 报错词典](docs/06-error-dictionary.md) —— 每条报错的**源码抛出点**与真实成因(四条报错性质完全不同,我一开始归成了一类)
- [07 · 三份 Movements 的战争](docs/07-three-movements.md) —— `collectblock` / `pvp` / 你自己各有一份;`canDig` 会**静默吃掉整个功能**
- [08 · `Promise.race` 的定时器泄漏](docs/08-promise-race-timer-leak.md) —— 一颗延迟 30 秒才炸、而且炸在别人身上的雷,以及怎么抓到它
- [09 · 插件选型与公网端口](docs/09-plugins-and-ports.md) —— 三个默认在公网裸奔的调试界面,和三条「照做会出事」的社区建议

## 现状(诚实版)

**能做到的**:自己砍树、合成(木板→工作台→木棍→木剑)、自己摆工作台和箱子、
安家、存东西、打怪、躲弓箭手、饿了吃饭、卡住了自救、被点名才说话、
按人格决定给不给玩家东西、被打断的长任务能捡回来接着做。

**还做不到 / 已知不好**:
- **不会盖房子** —— 目前只有"在脚边地面放一块"的原语,没有往上叠的能力(在做)
- **死亡率没有真正下降** —— 见上面那张表的最后一行
- `bot.on('death')` 重复触发还没根治
- `explore` 占了 57% 的决策,而且经常"只挪了 2~10 格"
- 本地小模型的游戏知识不可靠(3B~14B 全错),得靠"命中就原样吐出标准答案"绕过

## 关于这份代码

发布前做过脱敏:内网地址换成了 `127.0.0.1` / `<AI-GATEWAY>`,真人玩家的账号名换成了 `<OWNER>` / `<PLAYER>`。
**代码里没有任何密码或密钥** —— 登录凭据一律走环境变量(见 `.env.example`)。
脱敏只动了注释、三个永远不会被求值的兜底默认值(环境变量优先),和一条日志文案;
逻辑一行没改,脱敏前后行数完全一致,`node --check` 全部通过。

## 免责

这是一个真实项目的快照,不是教程,也不保证适合你的服务器。
里面的数字都来自一台具体的机器和一个具体的世界,换个环境不一定复现。
