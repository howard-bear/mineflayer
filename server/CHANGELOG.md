# 羊羊服务器变更日志

> **这个文件存在的唯一理由是 Owner 的一句话:「以后好查询 不然你很快就会失忆的」。**
>
> 维护这台服务器的是 AI 助手,而助手的会话记忆会被压缩 —— 今天查清的东西,下一次可能就没了。
> 所以规矩是:**服务器的每一次改动,都要在这里留一行。** 包括改了哪个文件、改前改后、以及**怎么回滚**。
>
> 时间一律用 **JST**(服务器所在时区)。带 `md5` 的地方是为了让下一次能确认"线上跑的是不是这一版"。

---

## 怎么读这个文件

每条至少要有四样东西,少一样就是没写完:

| 要素 | 为什么必须有 |
|---|---|
| **改了什么** | 半年后你不会记得 |
| **改了哪个文件** | 不写就得重新翻一遍服务器 |
| **为什么改** | 决定了以后能不能放心改回去 |
| **怎么回滚** | 出事的时候没人想现场推理 |

---

## 2026-09-17

### 🆕 资源世界:第 6 次起每次 1 金币

**为什么**:Owner 要"象征意义地"收一下,让每天第 6 次之后的随机传送有点成本。

**改哪儿**:`plugins/DeluxeMenus/gui_menus/resworld.yml`
(**不是** ResWorld 插件本身 —— 插件源码已经找不到了,只剩 jar;而且它不依赖 Vault、自己收不了钱、
超过 `daily-limit` 会硬拒,所以只能在菜单层做。)

**怎么做的**:免费按钮(钻石矿石)和付费按钮(金锭)**占同一个格子**,靠 `view_requirement` 二选一:

```yaml
# 免费:%resworld_left% 不等于 '0'
# 付费:%resworld_left% 等于 '0' → 要求 has money 1 → [givepermission] 临时不限次 → res go → 收回
```

`ResWorld/config.yml` 的 `daily-limit: 5` **没有改动** —— 那 5 次仍然是免费额度。

**🔴 上线后立刻出过一次事故,值得单独记**:
第一版**漏了 `priority` 字段**,结果**按钮直接从菜单里消失**,孩子反馈"传送按钮没了",线上坏了约 20 分钟。
DeluxeMenus 官方示例(`requirements_menu.yml:180-229`)写得很清楚:

> `priority` is used in case you have multiple items on the same slot. A lower number equals a higher priority.
> When the requirements aren't met and an item with lower priority occupies the same slot, will it be displayed instead.

同一格子放多个 item **必须写 `priority`**(数字小 = 优先级高)。现在是免费 `priority: 0`、付费 `priority: 1`。

**回滚**:
```bash
# 回到没有收费的原始菜单
sudo cp plugins/DeluxeMenus/gui_menus/resworld.yml.bak-paid-20260917-192730 \
        plugins/DeluxeMenus/gui_menus/resworld.yml
sudo mc-rcon "dm reload"
```

**顺带查清的两条**(以后别再重新试错):
- 本服 **LuckPerms 没挂 PlaceholderAPI 钩子**(活跃钩子里没有它),所以用不了 `%luckperms_*%`
  来判断权限,也没法用它验证 `lp permission settemp` 的效果 → 改用 DeluxeMenus 自带的
  `[givepermission]` / `[takepermission]`(它们是插件内部同步执行的)。
- **`%vault_eco_balance%` 和 `/balance` 的数字对不上**(placeholder 返回 `5.69`,命令说 `$805.68`)
  → 所以**没有**把余额写进菜单说明。给孩子显示一个错的余额比不显示更坏。

---

### AI 玩家小麦:换到 Node 22

**为什么**:mineflayer 从 4.26.0 起声明 `engines.node >= 22`,而 Debian 13 只带 20 —— 一直低于最低版本。

**改哪儿**:systemd drop-in `/etc/systemd/system/mcbot.service.d/node22.conf`。
**没有**加第三方 apt 源、**没有**碰系统的 `/usr/bin/node`(仍是 Debian 的 20)。
官方 tarball 解压到 `/usr/local/lib/nodejs/`,sha256 对过官方 `SHASUMS256.txt`,稳定软链 `node22`。

**回滚**:
```bash
sudo rm /etc/systemd/system/mcbot.service.d/node22.conf
sudo systemctl daemon-reload && sudo systemctl restart mcbot
```

顺带:部署脚本的语法关原来用 `node --check`(拿系统的 v20 去检一份实际跑在 v22 上的文件 ——
**用旧解释器检新语法会静默放过**)。已改成从 systemd 单元里抠出真正的解释器。

---

## 2026-09-17 地图「刷新很慢」的真因是浏览器缓存,不是渲染

**改了什么**:地图瓦片的 HTTP 缓存从 **24 小时压到 60 秒**。

**为什么**:Owner 问「bluemap 刷新很慢,新区域多久刷出来」。量下来渲染侧一点问题都没有:

| | |
|---|---|
| 存盘 → BlueMap 排队 | 约 5 秒 |
| 排队 → 瓦片写出 | 实测到 0.7 秒 |
| 10 分钟写出瓦片 | 主世界 1745 块,全部地图约 2400 块 |
| 渲染线程 | 两条都钉在 99.9%(**满载 ≠ 落后**) |
| **真因** | BlueMap 自己发的 `Cache-Control: public, max-age=86400` |

服务器 5 秒就把新区域画好了,浏览器却整整一天不再问一次 —— 所以看起来像"地图不刷新"。

**哪个文件**:`/etc/nginx/sites-available/bluemap`,在 `location / {` 之前插一个
`location ~ ^/maps/`,把 BlueMap 的 `Cache-Control` 换成 `max-age=60`。

**为什么是 60 不是 0**:0 会让每次平移都重下全部瓦片;地图本来也不是秒级更新的东西。
**边界(已验证)**:只影响 `/maps/`;`/assets/index-*.js` 仍是 `max-age=86400`,没误伤。
**验证**:`curl -sI map.v2good.com/maps/world/tiles/...` → `max-age=60`。
**回滚**:`cp` 回 `bluemap.bak-cache-20260917-230348` → `nginx -t && systemctl reload nginx`。

> ⚠️ 差点去调 `render-thread-count`。而本仓库里那条「调到 6 会吃掉 11/16 核让玩家卡加载地形」
> 的教训是**对的** —— 正因为它对,它让错误的假设显得有依据。

---

## 2026-09-17 地图网页上能看到游戏内聊天(只读,单向)

**改了什么**:BlueMap 网页左下角加了聊天小窗,显示游戏里的公屏发言。

**方向只有一个:游戏 → 网页。** 网页那边不能说话 —— 地图是公开无密码的,
双向会让任何拿到网址的陌生人直接对着孩子发消息。Owner 明确只要只读这半。

**🔴 安全判据用白名单,不用排除法。** 只放行 `Async Chat Thread` 那一种日志行:

```
^\[(\d{2}:\d{2}:\d{2})\] \[Async Chat Thread - #\d+/INFO\]: (?:\[Not Secure\] )?([A-Za-z0-9_]{1,16}): (.+)$
```

理由不是"白名单更严",是**两种错误的代价差了几个数量级**:
白名单漏一条 = 少显示一句聊天;黑名单漏一条 = **密码上公网,且不可逆**。

> **`latest.log` 里当天有 76 条明文密码**(`/login` 42 + `/l` 40 + `/register` 34)
> 和 65 行私聊,全部被丢弃。**任何"把日志内容展示/转发/喂给模型"的功能都要先过这一道。**
> (顺手查过:AI 玩家那两个进程的日志里 `/login|/register` 后带参数的行 **0 条**,
> 它们只记「发送 /login」这个固定串,不记原文。)

**新增的东西**(一个字都没改 BlueMap,所以 BlueMap 升级冲不掉):
`mc-chatweb.py` + `mc-chatweb.service`(`ProtectSystem=strict`、`MemoryMax=128M`)+
`/var/lib/mc-chatweb/`(chat.json 最近 60 条 + 前端 js)+ nginx 两个 `location`。
渲染一律 `textContent`,沙箱里用 `<script>alert(1)</script>` 验过。

**两个 bug,都不抛错,形状值得记:**
- **日志轮转后静默停更**:文件换了之后重新打开却 `seek(END)`,新文件是空的 →
  轮转瞬间写进去的行永久漏掉。我们每天 05:00 重启会轮转一次 →
  表现是「每天早上聊天窗死掉」。
- **存满 60 条后永久停更**:用"消息条数变了没有"判断要不要写盘,
  而存满上限之后长度永远不变 → 前 60 条正常,第 61 条起再也不动。

> **共同点比 bug 本身重要:两个都不报错,坏掉的样子和"大家没在说话"是同一个画面。**
> 所以界面上加了「更新于 hh:mm:ss」—— **让沉默的失败有个样子。**

---

## 2026-09-18 网页可以往游戏里说话(需后台配的密码,默认关)

**改了什么**:地图网页的聊天窗加了输入框;后台新增「网页发言」页,配「一个密码 = 一个名字」。
**默认是关的** —— 后台不打勾,网页上连输入框都不出现。

**🔴 安全支点:发言者的名字【由后台指定】,不是请求里带的。**
网页只交一个密码,服务端把密码换成 Owner 配好的名字 → **冒充不了任何人**。
游戏里显示成 `[网页] 名字: 内容`。

**限速**:同一密码 2 秒一条、每分钟 15 条;全站每分钟 40 条。
**注入防护**:剥掉 § 颜色码和控制字符、长度 120;`tellraw` 用 json 转义;网页渲染走 `textContent`。
**发言日志**:`/var/lib/mc-chatweb/send.log`(时间 / 名字 / IP / 内容)。

**🔴 一个关于文件权限的坑,值得单独记:**
`/opt/minecraft` 是 `drwx--x---`,新建的专用账号连穿都穿不过去。
第一版加了 `setfacl -m u:mcchat:--x /opt/minecraft` —— 然后自检发现
**它顺带能读到所有 world-readable 的插件配置了**(里面可能有别的密钥)。

> 一条"只加了执行位"的 ACL,实际放开的是它下面每一个 0644 文件。

改用 systemd 的命名空间而不是靠文件权限:
`TemporaryFileSystem=/opt:ro` + 三条 `BindReadOnlyPaths`(只给 logs、server.properties、密码表)。
验过:`nsenter` 进它的命名空间,`latest.log` 能读、插件配置**看不见**了。

**已知限制**:`mc-chatweb` 重启后网页发的消息会从聊天窗消失
(它们只在内存和 chat.json 里,而 seed 是从游戏日志重建的,`tellraw` 不写日志)。
游戏里的人看到过了,只是网页历史里没了。判断不值得为它加合并逻辑,但记一笔。

**还没解决**:Owner 说「AI 也可以(看到网页的话)」。现在用 `tellraw` 送进游戏,
**AI 村民老麦多半听不到**(它听的是玩家聊天事件)。要让它接上得改 McAI 插件。

---

## 2026-09-18 Owner 让暂停圈地,以及"会改变别人处境的能力要有默认关闭的开关"

**Owner 原话**:「紧急任务, 小麦家的领地太靠近玩家了, 你先取消掉小麦的所有领地」
→ 随后「让他暂时先别圈了」。

**已执行(服务器侧)**:用 `griefprevention:deleteallclaims XiaoMai` 删掉了它名下两块
(老家那块 + 当天圈的那块),圈地格数 **3772 → 4284 全额退回**。
**孩子们的 21 块领地一块没动** —— 逐文件核对过。
整个 `ClaimData` 目录已备份(24 个文件),要还原随时可以。

**已执行(机器人侧)**:两道闸,缺一不可。

| 闸 | 拦什么 | 生效方式 |
|---|---|---|
| `scripts.json` 里两条脚本 `"开": false` | 脚本层不会再挑中它 | **热加载,不用重启** |
| 代码里 `CLAIM_ENABLED`(默认关) | 硬规则绕过脚本层直接派动作的那条路;动作入口本身 | 要部署 |

> **为什么两道都要**:硬规则是**绕过菜单和脚本层**直接塞动作的,
> 只关脚本挡不住它。而只靠代码开关的话,改一次要部署一次。
>
> **更要紧的是:不能靠 `land.json` 里那个 `done: true` 碰巧挡着。**
> 那个标记现在是**过期的**(文件说圈好了,服务器上已经没有了)——
> 一旦它被重置或被我"修正",机器人几分钟内就会圈回去,
> **把主人的处置直接抵消**。所以启动时会如实打一行:
> 「land.json 说地盘圈好了,但圈地功能现在是关着的……这个标记可能已经过期」。

**服务器侧还加了权限闸**(平级会话做的,磁盘导出验证过):
两个 AI 玩家的 `griefprevention.createclaims` 和 `griefprevention.claims` 都设成 false。
覆盖了"放箱子自动送地"和"金铲子划两角"两条路。

⚠️ **副作用**:否掉 `griefprevention.claims` 会连带禁掉同节点下一整排命令 ——
`/trust`、`/untrust`、`/containertrust`、`/abandonclaim`、`/claimslist` 等。
所以「全家共享」那条 `/trust` 现在也会失败,已经跟着一起关了。

⚠️ **`/claim` 不带半径这条路,权限闸拦不住**(`plugin.yml` 里它挂的节点
默认 `true`)。现在靠机器人侧那两道闸挡着。要做到"永久、覆盖全部路径(含第三方 API)"
得写插件监听并取消 `ClaimCreatedEvent` —— 只有 Owner 说"永久不许圈"时才值得做。

⚠️ **LuckPerms 在这台机器上既不给 RCON 回执、也不写 latest.log。**
命令其实执行了,但完全看不到。唯一的验证办法是 `lp export <名字>` 然后读导出的 json.gz。
**任何靠 `lp` 改权限的代码,现在都是瞎的。**

**这一条的通则**:
> 机器人**会改变别人处境**的能力(圈地、给权限、发消息、花钱),
> 必须有一个**默认关闭**的开关,而且开关要在"绕过上层的那条路"上也生效。
> 否则主人的一次处置,会被机器人几分钟内抵消 —— 而它并不知道自己在对抗主人。

---

## 2026-09-18 老麦:服务器上没有真人时不再自言自语,也不理 AI 玩家(McAI 1.8.0)

**Owner 原话**:「我觉得服务器没有真人的时候 让老麦就别说话了 纯浪费算力」。

**查下来的真相**(平级会话数的):老麦 164 句话里 163 句是**写死的闲聊台词**(不调模型),
其中 56 句是对着一个真人都没有的服务器说的;模型现场生成的回答只有 1 句。
所以闲聊本身几乎不耗算力 —— 真正的毛病是它**把机器人当成了玩家**:
服务器上的 AI 玩家(小麦/麦娘/麦豆)以真人身份进服,插件分不出来,于是:
```
老麦今天心情特别好,见谁都想打招呼!MaiNiang 你好呀~
MaiNiang,你的房子圈领地了没?可别被熊孩子拆咯~
```
而这些现在都会出现在地图网页的聊天窗里。

**改了什么**:
- 主动闲聊前只数**真人**;一个真人都没有就跳过这一次(真人一上线就恢复)。
- 台词里点名(`%player%`)只从真人里挑,永远不点机器人。
- **AI 玩家在聊天里说的话,老麦一律不理**(不回答、也不听它指挥去哪儿)。
  这一条是审查时补的:**回答才是真正调模型烧算力的那条路** —— 机器人站在老麦 12 格内说一句话,
  原来就会触发一次模型调用。
- ⚠️ **不动的边界**:孩子问老麦照常回答;网页发言那边用 RCON 调的 `mcai ask` 不经过聊天入口,也照常回答。

**「谁是 AI 玩家」全服只有一份来源**:机器人自己的 `roles.json`(各角色的「登录名」)。
生成器 `mc-ai-regen` 每次都从那里重新算出 `ai-players:` 写进插件配置 ——
所以**后台怎么保存都抹不掉它**(这台服务器上"后台一保存就抹掉手工改动"已经发生过两次),
以后加第三个、第四个 AI 玩家,也只要在 `roles.json` 登记一处。
读不到 `roles.json` 时用现有配置里的名单,再不行才用写死的三个名字兜底。

**验证(带对照)**:
| 验什么 | 结果 |
|---|---|
| 插件载入 | McAI 1.8.0,0 报错;状态行显示「不算真人的AI玩家=[…] \| 此刻在线真人=0」 |
| 硬边界:只有机器人在线时问老麦 | ✅ 照常回答 |
| 没有真人的 27 分钟(10:37~11:04) | 老麦只说了 1 句 —— 就是上面那次提问的回答;**闲聊 0 句**(改之前这段时间应有 2~4 句) |
| **对照组**:临时把麦豆算成"真人"、间隔调成 1 分钟 | ✅ 1 分钟内开口,没点小麦、麦娘的名 —— 证明闲聊没被误关;看完立即还原 |

> 对照那一句恰好是不带人名的台词,所以「只点真人的名」这一点**没在对照里被直接走到**,
> 由代码审查覆盖(点名只从真人名单里挑)。

**顺带上线了 1.7.1 那条 `/mcai cleanup`**(全图清掉重复的老麦)。
它 9/15 就写好编好了,一直在等服务器重启,**从来没装上去过** ——
本地源码比线上超前一个版本这件事,当时没有记在任何地方。
这次部署前查清了它改了什么,才一起上线;**不查就直接推,等于顺手发布了一个没人复核的改动。**

**另一件值得记的**:插件源码原来**只存在于某个旧会话的 `/private/tmp/…` 临时目录里**,
随时会被清掉,那样就只剩编译好的 jar。现在搬到了 Owner 本机的项目目录。

**重启时机**:换插件必须重启整个服务器。部署时是日本时间 10:36,**在线的只有两个机器人、一个真人都没有**,
部署脚本第一步就是「有真人在线就中止」。用的是服务器自带的标准重启(游戏内预警约 60 秒)。

**回滚**:`/opt/minecraft/plugins/McAI.jar.bak-20260918-103635`、
`/usr/local/bin/mc-ai-regen.bak-20260918-103635`、`plugins/McAI/config.yml.bak-20260918-103635`,换回后重启服务器。

---

## 2026-09-18 全家共用文件里挂着一块已经删掉的领地

凌晨按 Owner 的指令删了小麦的领地、关了圈地,但 `family.json` 里还一直写着那块地。
原因是:小麦每次启动都会把"我圈的地"登记进全家共用文件,而它判断"圈过没有"看的是
自己的 `land.json` —— 那个标记**是过期的**(文件说圈好了,服务器上已经没有了)。
于是每次重启都把一块不存在的领地重新登记一遍,麦娘、麦豆一上线就读到
「家里的地盘是小麦圈的……我还不在白名单里,得等他授权」,而且这段话会喂给模型。

**改法**:圈地开关关着的时候,不登记领地,并把自己以前登记的那份清掉;
喂给模型的「家里人」也不再提"能不能进我地盘"—— 没有领地,这个问题本身就不存在。
**验证**:`family.json` 的 claim / claimBy / trusted 都已为空,箱子和房子信息还在;
麦娘、麦豆重启后读到过期领地的次数都是 0;三个进程 0 硬错。

> 同一个过期标记今天咬了两次:一次是差点让它把领地圈回去(已用开关挡住),
> 一次是这里 —— 它被当成事实广播给了全家。**一个已知过期的标记,不能只在一个地方挡住。**

---

## 2026-09-18 麦豆(小麦的儿子)上线

> 这条由平级会话按 Owner「把麦豆也上线吧」执行,我代为记录。**没有改任何机器人代码**,只加了运行配置。

- 和麦娘同一套:`mcchild.service`(比麦娘优先级再低一档)+ `/etc/mcchild.env`(只有 root 能读,
  登录凭据为它单独随机生成、不打印、不进仓库)+ 自己的数据目录 `data/child/`。
- 它**自己完成了注册和登录**,上线后公屏发言 0 句。
- 它的动作清单里**没有"安家"** —— 所以预置了它的家 = 全家那座房子(孩子跟爸妈住),
  并在游戏里替它 `/sethome base` 了一次(第一次上线出生在世界出生点,离家 146 格,
  自带的"离家太远就传送回家"规则因为它还没有家而失败)。
- 圈地权限和小麦、麦娘一样设成不允许(它本来就没有圈地的动作,这是保持一致)。
- **回滚**:`systemctl disable --now mcchild`。

**上线后暴露的两个设计问题(还没改)**:
1. **"跟着爸爸去林子看他砍树"对新角色永远开不了工** —— 开工条件是"记得林子",
   而新角色的地图是空的。要么让它继承家里人记得的林子,要么把条件改成"爸爸在线且在林子里"。
2. **规划师在给麦豆派它做不了的活**(「去林子砍树」「存木板进箱子」—— 它既不会砍树也不会存东西),
   见下一条。

---

## 2026-09-18 🏃 麦豆遇怪只跑不打(Owner 拍板第 11 件)+ 几处孩子安全 —— 16:15 JST 上线

Owner 15:14 拍板第 11 件,麦豆「遇怪只跑不打」。平级会话 fork 2 数过:今天 10:55~15:52 JST 麦豆还手 78 场,用过石斧、石剑;
T0 上线后的 20 分钟里「正在还击」又有 40 行,被溺尸拖到离家 58 格。

> **16:14 JST 更新**:Owner 在 fork 2 的会话里又拍板了(选项原文):「三个人一样:手上有剑、血够、只有一两只普通怪(僵尸、蜘蛛这类)才打;
> 苦力怕、怪太多、在水里、血少时一律跑」,并主动说「我建议是所有角色都要有武器装备」;另外「做 3 把钻石剑」(用箱子里 6 颗钻石,三人各一把)。
> 这**取代**了第 11 件里「麦豆只跑不打」那一半,也定了第 10 件(麦娘还手)。
> 所以这一刀是**过渡状态**:先让空手的麦豆不再硬打;等三把剑到手、「按规矩才打」的判断做好,再换成对三个人一样的规矩。

**这一刀只动麦豆**(角色名单 `NO_FIGHT_ROLES = {child}`,不按动作表):
麦娘的动作表里也没有 fight,但写这一刀时她要不要还手(第 10 件)Owner 还没定,所以原样不动(反射照旧会还手)。
16:14 定了之后,她和小麦也要换成上面那条「按规矩才打」—— 那是后面单独的一刀(小麦现在也会空手硬打,换规矩对他也是真变化)。

| 改了什么 | 为什么 |
|---|---|
| 反射层所有「打」的出口(还手、贴脸冲上去打弓箭手、低血转身近战)对麦豆一律改成跑 | Owner 定的 |
| 麦豆**每一拍都跑**,近战怪进 6 格就跑 | 审查抓到的阻断级问题:三处反射都是「同一只怪只下一次指令」—— 打的时候没问题(插件自己追着打),改成跑以后只跑一次,跑到 24 格外停下,同一只僵尸追上来就站着挨打。复核又补了 4~6 格的空档(苦力怕 3 格内点火) |
| 麦豆不放挡箭方块 | 挡箭反射会在任何地方放方块(fork 1:06:07:48 放过一块原木,另一次被别人的领地挡下),碰「只在家附近放方块」;小麦照旧 |
| 家被标成不安全的 5 分钟里,麦豆不去出生点、血少时先跑 | 出生点离家一百多格(目标表 M7-1「别走丢」);原来这 5 分钟里她的 `/home` 会被换成 `/spawn` |
| 这 5 分钟里「回家」如实说原因 | 原来等 4 秒后报「/home 没起作用(可能在冷却)」,原因是假的 |
| 「离最近的真人多远」出错时当成「有人在旁边」 | 原来出错返回「无穷远」= 出错就放行麦豆出门玩 |

**从这一刀里拆出去的**(fork 1 建议:会动小麦、麦娘,量法也不同,混在一起分不清是谁的功劳):
- 收割、割草只捡自己刚弄出来的掉落物 —— 单独一刀;
- 「接回被插队的活一律从第 1 步重来」—— **暂缓**。Owner 09-17 23:22 说过「因为紧急事件被打断的任务要恢复起来继续完成」,
  而做剑、做锄头的「做木棍」那一步没有跳过条件,从头来会重做木棍、多耗木板。先说明清楚再定。

⚠️ **这一版带上线了一行说错的日志**:拆补丁时,「接回从第 1 步」的【日志那一半】误留在了这一刀里(行为那一半没上)。
所以从这一版起,「↩️ 接回被插队的…」一律写「从第 1 步重来,已经满足的步骤会跳过」,**实际仍是从挂起的那一步接着做**
(离挂起的地方超过 32 格才从第 1 步)。我做的「拆开后三份拼回去和整份逐字相同」的检查查不出这种放错位置的问题。下一刀(氧气)顺带改回。

审查:两轮。第一轮(fork 2 的 4 个视角之外另起一个复核员)抓到「只跑一次」的阻断;第二轮确认小麦、麦娘在这几处的行为和线上逐字相同,补了 4~6 格空档。

**实测**(前后各 20 分钟;UTC 06:54:47~07:14:47 / 07:17:30~07:37:30,即 JST 15:54:47~16:14:47 / 16:17:30~16:37:30;
fork 1 用自己的窗口 06:48:55~07:08:30 / 07:15:40~07:35:20 独立复核,结论相同):

| 麦豆 | 改前 | 改后 |
|---|---|---|
| 「⚔️ 正在还击」 | 28 | **0** |
| 🏃「不打架,改成跑开」 | 0 | 8 |
| 死 | 4 | **0** |
| 小目标达成 | 5 | 9 |
| 执行失败 | 5 | 2 |
| 🧱 挡箭放方块 | 0 | 0(改前她身上本来就没原木,这一项证明不了什么) |
| 最远离家 | 146 格 | **157 格** ⚠️(见下) |
| 网关调用(三人合计) | | 23 分钟 2 次 |

⚠️ **对照组变化很大**:同一时段小麦、麦娘的「⚔️」从 36 / 40 降到 12 / 9 —— 改后窗口里怪少得多。
死亡 4 → 0 不能全算这一刀的功劳;能确定的是代码层面她不再还手(两轮审查 + fork 1 逐块比对:13 处改动,除了那行日志没有混进别的)。

🔴 **新问题(fork 1 抓到,高)**:**逃跑没有范围上限。** UTC 07:21:22 她刚传送回家 (-17,63,159),
07:21:49~07:23:12 四次「僵尸 4~6 格,跑」,07:23:18 拴绳报离家 **157 格**(-139,66,59)—— 116 秒、夜里。
逃跑每一拍都往「当前位置 + 背离怪物 24 格」定目标,不看家在哪;跑的时候大脑被按住,兜底和拴绳都插不上手。
这和她的大目标「跟着大人学,别走丢」、和这一刀不让她去出生点的理由正好相反。**下一刀修**(围着家跑,不跑远)。

另外 fork 1 还指出:「在家附近玩」做到 20 格就算完成 → 兜底回家线也是 20 格 → 传送回家 → 再去玩,改后窗口里来回了 6 次(不伤人,但一直在传送);要改表(交给 fork 2)。

**版本**:`bot.real.js` `1bb03e8f…` → `7ec16f3a…`;备份 `bot.real.js.1bb03e8f-before-safety-20260918-071449`。
**回滚**:换回备份,三个服务各重启一次。

---

## 2026-09-18 🕳️ 没活干时照目标表做(T0 兜底)—— 15:46 JST 上线;麦娘因此变差,下一刀修

Owner 15:14 拍板第 1 件:「没活干 → 按目标表里写好的来」(J-T0-1/2/3)。
一件小目标都开不了工时,**不问模型**,由代码按下面的顺序挑一件:

1. 离家太远先回家:小麦、麦娘超过 28 格,麦豆超过 20 格;30 秒最多一次;家被标成不安全、或 go_home 连败 3 次时不回。
2. 血没满、饱食不到 18、身上有吃的 → 吃一口(10 秒最多一次)。
3. 下面这些 20 秒最多一件;离家太远又回不去时一件都不做:
   - 麦豆:白天、血多于 15、离家 10 格以内、24 格内没有真人 → 在家门口走走;
   - 小麦、麦娘:家里箱子的摘要旧了 10 分钟以上 → 回去看一眼(新动作 `check_chest`,只看不拿;`roles.json` 给两人放行);
   - 麦娘:小麦 ≥3 → 做面包;有粮箱并且(面包 ≥4 或小麦 ≥12)→ 存粮;
   - 小麦:房子还缺料时什么都不存;
   - 身上有可存的东西 → 存进箱子。

**和 Owner 选的那段原文对不上的地方**(平级会话 fork 1 复核列出;原样记下,目标表还没改):

- a. 「小麦不缺就回家存东西」:代码是房子还缺材料时,T0 **什么都不存**(不只是木料)—— 现在等于关着。
- b. 「小麦缺林子就出去找一圈」:T0 里没有这一步,靠的是「找林子」脚本;它冷却的 2 分钟里小麦在家站着(UTC 06:50:53→06:52:47、06:55:16→06:56:18)。
- c. 数字:表里回家是「离家 ≤8 / ≤16 不回」,代码是超过 28 / 20 才回(`/home` 的落点本身就离家里记的点十几格);走动半径表里 16、代码 10;两次动作之间隔 20 秒;只看大箱子,没看粮箱。
- d. 麦豆的回家线 20 格和「在家附近玩」的完成条件「离家 ≥20」贴在一起,要留意会不会出现 20 格上来回。

**实测**(前后各 20 分钟;UTC 06:25:54~06:45:54 / 06:48:37~07:08:37,即 JST 15:25:54~15:45:54 / 15:48:37~16:08:37;小麦 / 麦娘 / 麦豆):

| | 改前 | 改后 |
|---|---|---|
| T0 兜底动作 | 0 / 0 / 0 | 6 / 6 / 11 |
| 小目标达成 | 0 / 2 / 1 | 3 / 2 / 3 |
| 执行失败 | 33 / 3 / 0 | **48 / 20 / 7** ⚠️ 变差 |
| 回家失败(「/home 没起作用」) | 0 / 0 / 0 | 1 / 6 / 3 |
| 回家失败(「动了 N 格但没到家」) | — | 0 / 4 / 4 |
| 死 | 0 / 1 / 4 | 0 / 2 / 4 |
| 网关调用 | | 20 分钟 4 次 |
| 硬错 | 0 | 0 |

**变差的原因**(逐行看了日志):

- **麦娘 —— 是 T0 造成的回归**:T0「存东西」把她刚从箱子里拿出来的木板又存了回去(窗口里存了 3 次、每次 1 个),
  「从箱子里拿木板」拿了 10 次只成功 1 次。同一个判据有两份实现:缺口日志按「可存的东西 ≤1」算已经存好,T0 却按 ≥1 去存。
  **下一刀修**:用脚本自己的门槛(麦娘 ≥3、小麦 ≥4),正在攒的东西不存。
  另外 07:02:57、07:03:32(UTC)她在家门口连死两次 → 家被标成不安全 → `/home` 被换成去出生点 → 回家连着失败(这是原来就有的逻辑)。
- **麦豆**:T0 让她在家门口走走(06:51:25),两秒后开始和溺尸打(窗口里「正在还击」40 行),被拖到离家 38~58 格,回家失败。
  Owner 定的是「遇怪只跑不打」,下一刀(安全修正)修的就是这个。
- 小麦、麦娘的「正在还击」也各有 42、40 行;麦娘要不要还手是 Owner 还没定的第 10 件,**不动**。

⚠️ 只有 20 分钟、数小。能说的是:没活干时不再问模型、兜底动作确实在做;麦娘的存取来回是真回归。

**版本**:`bot.real.js` `24c9b4e5…` → `1bb03e8f…`;`roles.json` `eae2d747…` → `30b884a7…`(小麦、麦娘放行 `check_chest`)。
备份 `bot.real.js.24c9b4e5-before-t0-20260918-064557`、`roles.json.bak-20260918-064557`。

**回滚**:换回这两个备份,三个服务各重启一次。

---

## 2026-09-18 🔧 ③b 遇事才想的修正(1 个回归 + 3 个循环)—— 15:23 JST 上线

平级会话 fork 1 用 6 个复核员核了「遇事才想」上线后的日志,「没看到坏处」不成立。都不伤玩家,修前进,没回滚:

| 问题 | 真因 | 修法 |
|---|---|---|
| **回归**:小麦连着 16 次砍树失败都被当成「险情打断」放过,「失败 3 次就放下」被绕过 | 判据里有「险情还没结束」,而泡水时险情一直开着(入水反射只在前 10 秒压住大脑) | 只认两种:**这一步执行期间反射真压住过大脑**、刚死过;**连续豁免最多 5 次**,一步做成就清零,用完说一声 |
| **循环 1**:麦娘兜底回家每 40 秒一次、永远到不了 | `/home` 落点离家里记的点约 16 格,正好卡在「离家 16 格就回家」上,而 go_home 判到家也是 16 格 | go_home「到家」改成 **20 格**(给落点误差);兜底回家要超过 **28 格**;go_home 连败 3 次、家被标成不安全时不兜底 |
| **循环 2**:麦豆「玩到 20 格 → 送回家 → 再玩」 | 两个阈值没对齐 | 同上;另外 `child_home` 的完成条件「离家≤8」改成「≤20」,和 go_home 判到家对齐(原来会连着 /home 7 次再被判失败) |
| **循环 3**:「找林子」反复被「凑盖房材料」抢走,而后者正是因为附近没树才连败 | 刚失败过的活又被当成「新冒出来的更要紧的事」 | **10 分钟内最近一次是 ✗ 的活不算新冒出来的**;保命、安全两个大目标的活不受这条限制(按大目标 id 判,不按数字 —— 麦娘的「全家有饭吃」也是 20) |

另外:
- 接活那一行把**没选的候选全列出来**(名字 + 优先级),复核员从日志就能还原「模型在哪几件里挑的」;
- 睡觉补时间只补**真睡着的那段**,用反射层每秒记的「醒着」来分段(第一版用「两次调用隔了 5 秒」判断,复核指出硬规则每 15 秒插一次强制睡觉,会让睡觉时间少补三分之一 —— 挡下来了);
- 睡着时刷新「最后挪动时刻」,醒来不再误报「75 秒没挪窝」;
- 「到家」半径统一用一个常量 `HOME_OK_R`(go_home 判到家、「家挪没挪」两处),取整方式和判据「离家距离」一致。

**版本**:`bot.real.js` `717be6fb…` → `24c9b4e5…`;`scripts.json` `4655ecf3…` → `f91d28b3…`(child_home);
备份 `bot.real.js.717be6fb-before-evt3b-20260918-062252`、`scripts.json.bak-20260918-062252`;
上一版的 `bot.real.js.359160d3-before-evt-20260918` 也补存了(平级会话提醒:原来只剩会被覆盖的 `.prev-safe`)。

**实测**(前后各 20 分钟,JST 15:02:49~15:22:49 / 15:25:33~15:45:33):

| | 改前(小麦/麦娘/麦豆) | 改后 |
|---|---|---|
| go_home 失败(「/home 没起作用」) | 3 / **12** / 9 | **0 / 0 / 0** |
| 更正(fork 1 复核):把「动了 N 格但没到家」也算进回家失败 | 4 / 17 / 11 | 0 / **2** / 0 |
| 兜底回家次数 | 8 / 16 / 7 | 3 / 5 / 3 |
| 险情豁免(不记失败的步骤) | **12** / 0 / 1 | 2 / 1 / 0(没有到上限的) |
| 被「刚失败过的活」抢走 | 小麦 2 次(06:08、06:10,插队的活 8 秒、36 秒就结束) | 0 |
| 执行失败 | 51 / 17 / 11 | 37 / 4 / 0 |
| 「我死了」(含未核实) | 1 / 1 / 2 | 0 / 0 / 3 |
| 网关调用 | — | 4 次 / 20 分钟 |
| 硬错 | 0 | 0 |

⚠️ 仍然只有 20 分钟、数小,只能说这四件修到了,「整体变好了」证明不了。

**回滚**:换回 `bot.real.js.717be6fb-before-evt3b-…` 和 `scripts.json.bak-20260918-062252`,三个服务各重启一次。

---

## 2026-09-18 📋 Owner 拍板的三件事(约 15:14 JST,用选择题问的)

> 前两件由平级会话 fork 1 在它的会话里问的,第三件由 fork 2 问的;下面是原问题(选项原文)和 Owner 的选择。

**1. 没活干的时候做什么** → Owner 选「**按目标表里写好的来**」。
选项原文:「天黑就回家;回家看一眼箱子;小麦缺林子就出去找一圈,不缺就回家存东西;麦娘把小麦做成面包、存东西;
麦豆在家门口离人远的地方玩。一有能干的活马上回去干。」
→ 就是目标表 README 里的 **T0** 和 `J-T0-1 / J-T0-2 / J-T0-3`。补全目标表之前照这三条做兜底(下一刀实现;实现之前是「回家/吃/歇着」)。

**2. 孩子说的话能不能改变机器人手上的事** → Owner 选「**能,由 AI 按性格决定听不听**」。
说明原文:「比如孩子说『帮我砍树』,小麦可能放下手上的活去帮忙,也可能不理。机器人会更像真人,但也可能被孩子支使得团团转。
你以前说过『被玩家触发时可以让 AI 选下一步』。」
→ 目标表里「被玩家触发」的那条思考(T2/T10,以表为准)。底线不变:模型只在合法选项里挑;不碰别人的东西、只在家附近放方块、
角色闸这些护栏照旧由代码挡;未成年人安全那条不放松。(和 fork 2 原来的建议不同,以 Owner 为准。)

**3. 麦豆学什么(拍板第 11 件)** → Owner 选「**按建议全学**」:砍树、收麦子种回去、存箱子,背包满了自己存,遇怪只跑不打。
→ fork 2 照线上现成脚本给麦豆加了 4 条(收麦子、补种、在家附近砍树、背包满回家存),门槛照抄 child_play 的,没有新编的数字;
麦豆要放行 `gather_wood`、`harvest_crop`、`plant_seeds`、`store_items`(`follow_player` 先不放:「跟着爸爸」要等「只跟家人」的限制做出来)。

---

## 2026-09-18 🔔 遇事才想 —— 14:17 JST 上线

**Owner 的原话**(按时间先后;后一句否掉了前一句里「定时发送情况」那一半 —— **别只引第一句的前半把定时问模型加回来**):
- 2026-09-17 23:23 JST「我同意每隔一段时间发送目前ai玩家的情况给到ai, 但是如果情况没啥问题就继续上一个任务 直到完成呀」
- 2026-09-18 11:43 JST「对了 触发思考 也是需要单独的脚本的, 不能定时去让AI寻找本地模型进行思考」
- 2026-09-18(经平级会话转达)「规划师的工作实际上就是要判断 大目标 小目标 和对应的脚本如何执行 遇到紧急事件 和基本状态维持 这些事情之间的关系, 进而触发思考」
- 2026-09-18(经平级会话转达)「那就是设计出现问题了 一直没有事情做的情况仅会出现在 最完美的游戏大结局吧」
- 四层目标表的表头:「数字越低优先级越高,当优先级一致的时候由 ai 来选择」

**改了什么**(一句话:**事情之间的关系变了才想;情况没问题就接着做;模型只在同档多选时才问**):

| | 以前 | 现在 |
|---|---|---|
| 什么时候重新想 | 一件活做完、放下时 | 另外还有 6 种**跳变**:熬过险情、天黑、血跌破 12、饱食跌到 8、背包满、被真人点名(同一个判据和脚本 / 硬规则一致,只在变的那一刻触发一次) |
| 遇事之后 | 险情一过就把原来那一步**原样捡回**(两条路都在捡) | 先用代码核:手上这件还能做(安全门槛仍成立)、又没有**接活之后才新冒出来的**更高优先级小目标 → 接着做,**不问模型** |
| 关系真变了 | — | 不能接着做,或者冒出更要紧的 → 按大目标优先级由代码换;**同一档有好几件**才问模型,并告诉它为什么问 |
| 被更要紧的事插队 | — | 那件**挂起、留着进度**,插队的做完了原样接回(不问模型、不记失败、不冷却);挪远了就从第一步重来 |
| 被险情打断的那一步 | 算这件活失败一次(连着 3 次就放下 + 冷 2 分钟) | **不算失败**;险情里、睡着时耗掉的时间也不算进这件活的时间上限 |
| 一个小目标都开不了工 | 每一轮都问一次模型(单步决策),答案大多丢掉 | **不问模型**:记一条「🕳️ 目标表缺口」到日志和 `goal-gaps.json`(给 Owner 和我们修表);代码兜底 —— 离家 16 格以上就回家、血没满有吃的就吃、其余时候待着 |
| 睡着的时候 | 任务层照样发步骤(被「我正在睡觉」挡回),时间照样计 | 任务层整个等着,不判超时、不挑新活 |

**可观测**:每次重想一行 `🔔 遇事重想[…] … → 接着做 / 换成 …`;每小时一行 `🔔📊`,分得开「代码判定接着做 / 代码换活 / 问了模型 / 真换了活 / 挂起接回 / 目标表缺口」。

**脚本表的数据改动**(热加载的 `scripts.json`):
- `wife_take_planks`、`son_take_mats` 加了可选的「继续条件」:它们的「还要满足」会被自己的进度推翻(拿一次木板就不再 ≤1 了),遇事时按「继续条件」判断能不能接着拿;
- `child_play` 的「还要满足」加了「天黑了=0」:原来天黑时麦豆如果正好在家附近,会整夜在家和 20 格外之间来回玩。

**审查**:四路对抗审查(运行正确性、规格、回归、场景推演)挑出 36 条,全部不是会崩的问题,但有好几处「没做到说的那件事」;
改完第二轮复审 23 个复核员又挑出一批,再改;最后两轮定点复核。几处值得记下的:
- 「任何一个无关的事件(比如有人打招呼)都能推翻模型之前有意的选择」—— 所以只认**接活之后新冒出来的**更高档候选;
- 硬规则派的动作(背包满了去存东西)被险情打断后照旧捡回,只有小目标自己的步骤才交给重想;
- 「零候选就待着」会被「75 秒没挪窝」当成卡住、每 75 秒传送一次 —— 待着如实记成「歇着」;
- 麦豆零候选时原来靠模型选「吃」来回血,改成代码兜底吃;
- **普通选择点暂时没改成「按档选」**:照现在这张旧脚本表硬按档选,会把「守家」变成只要 8 格内有任何怪就必须去打,「从箱子里拿木板」会对着过期的箱子摘要反复失败 —— 这是旧表的问题,放到新目标表(v2)那一刀一起改。

**版本**(三个文件一起换,备份后缀 `20260918-051733`):
`bot.real.js` `359160d3…` → `717be6fb…`;`scripts.js` `1025c382…` → `d1faffc9…`(加了 `prioMap` 导出,向下兼容);`scripts.json` `23f61e0a…` → `4655ecf3…`。
三个机器人 14:17:43 / 14:18:03 / 14:18:23 JST 重启。

**实测**(前后各 20 分钟 = MC 一整天,各含一个夜晚;JST 13:57:33~14:17:33 / 14:20:14~14:40:14;
改后那段的夜里是在开窗前 65 秒、也就是重启后 85 秒开始的):

| | 改前 | 改后 |
|---|---|---|
| 网关调用(所有来源) | 约 60 次 / 20 分钟(12:32~14:17 的平均,没能单独量出这 20 分钟) | **3 次** |
| 模型失败(按累计数) | 小麦 13、麦娘 4、麦豆 11 | 0 / 0 / 0 |
| 接小目标 / 达成 | 34 / 15 | 17 / 5 |
| 放下(失败) | 19 | 11 |
| 死亡(机器人日志「我死了」,含未核实的) | 4 | 5 |
| 硬错 | 0 | 0 |
| 遇事重想 | — | 小麦 8(接着做 6、换活并挂起→之后接回 1、不能接着做又没别的只好放下 1)、麦豆 3(接着做 3) |
| 被险情打断、不记失败的步骤 | — | 24 |
| 目标表缺口 / 兜底回家 | — | 17 / 24 |

**「达成」从 15 掉到 5,拆开看**:
- 麦豆改前 5 次「在家附近玩」有夜里玩的,改后天黑不玩了(上面那条数据改动,有意为之);
- 麦娘改后窗口的前半段离家很远、种地轮次少了(7 → 3)。⚠️ 我原写「开窗时离家 160 格」不对:平级会话核出她 05:21:55 和 05:22:18
  在家附近死了两次,是「回家就死」那条旧保护把她送去了出生点;
- 小麦改前的「成功」里 37 次是「已经睡着了」这种空转,13 次是模型单步决策;改后模型决策 0 次。

> 🔴 **平级会话(fork 1)复核后的更正**:**「没看到坏处」不成立** —— 有 1 个回归、3 个循环(都不伤玩家,已在 ③b 修,见上一条):
> ① 泡水期间「险情没结束」,把挖不到、寻路超时这类正常失败也当成险情打断放过去了(小麦连着 16 次);
> ② 麦娘兜底回家每 40 秒一次永远到不了(/home 落点离家 16.x 格,正好卡在阈值上);
> ③ 麦豆「玩到 20 格 → 兜底送回家 → 再玩」来回转;④ 「找林子」反复被正因为没林子而连败的「凑材料」抢走。
> 另外改后那 3 次网关调用是**普通选择点**问的(遇事重想那一路是 0 次);复核员推断其中 2 次模型挑了低一档的砍树。

**所以只能说**:模型调用几乎停了(≈60 → 3),死亡没有看得出的变差;**「干活没变差」没有被证明** —— 20 分钟、数太小,
麦娘那段还混着一个「离家很远」的起点。以每小时的 `🔔📊` / `🧠📊` 为准,多看几个小时再下结论。平级会话会独立复核。

**已知还没解决的**(复核查出来、这一刀故意没动的):
- 普通选择点仍是「候选多于一个就问模型」,没按档(见上,放到 v2);
- `TASK_MODE=0` 回滚路径仍是每一轮问一次模型;
- 盖房子先问模型设计、后看有没有平地 —— 没平地时每一轮都会白问一次;
- 箱子里有什么的摘要只在「存东西」时更新,「从箱子里拿」不更新 → 拿木板会对着过期摘要反复失败;
- 反射接管混着别的暂停、核实过的死亡、长时间泡水之后,原来那一步仍可能在重想之前先发出去一次。

**回滚**:`scripts.js`、`scripts.json` 用备份(后缀 `20260918-051733`)换回;`bot.real.js` 用 `bot.real.js.359160d3-before-evt-20260918`
换回(⚠️ 最初写成「三个文件都用 051733 的备份」是错的 —— bot.real.js 那次没带时间戳备份,只剩会被下次部署覆盖的 `.prev-safe`,
平级会话发现后补存了带版本号的这份)。三个服务各重启一次。`scripts.js` 新旧版本都能和新旧 `bot.real.js` 搭配,
但**新 `bot.real.js` 不能配旧 `scripts.js`**(接活时要用 `prioMap`)。

---

## 2026-09-18 🧠 模型答砸了终于看得见 + 主脑掉线能自动切备用 —— 12:56 JST 上线

**起因**(规划师关闭那次的对抗复核顺带查出来的):`brainTick` 里有一句 `if (!action) { return }`,排在所有失败记账**之前**。
而模型答砸时恰好就是 `action = null`,所以:
- 模型超时、乱答,**一个字都不记** —— 日志里「0 个错误」不代表模型没出错;
- 「主脑连着 2 次没反应 → 临时切到备用入口」那套保护**永远不会触发**(Owner 夜里靠它);
- 挑脚本的失败也从来不进账。

**改了什么**(只让它看得见、让降级能触发;**不改选活方式、不改喂给模型的任何内容**):
- 记账挪到**真正调了模型的那四处**:单步决策、挑小目标、聊天回话、设计房子。原来放在出口那里分不清这一步是模型给的还是脚本步骤,
  而它唯一跑得到的那一支(「有动作就把失败计数清零」)会被脚本步骤不停清零,让「连着 2 次」永远凑不齐 —— 已删。
- 失败打一行 `🧠✗`(30 秒最多一行,被压住的次数带在下一行里)。
- 每小时一行 `🧠📊`:四种调用各问了几次、答上几次、没反应几次、答不对几次、平均几秒。
- 「连着 2 次没反应」必须真的是连着:两次隔了 5 分钟以上就从头数(脚本模式下模型调用很稀疏)。
- 备用入口自己的失败不算到主脑头上(要按**发出去时**用的是哪个入口来判,不能按结束时刻,复核查出来的)。
- 「连着答不出就代码替它乱走一个方向」那条老兜底原来就跑不到,**仍然不让它动手**。

**被复核否掉、没做的一处**:我起初还想把「上一轮答不对」写进下一轮提示词,独立复核指出三处毛病:
会盖掉一个模型还没看到的真实动作结果(想和做是并行的);挑脚本的提示词也会读到它,而那句话和挑脚本的规则打架;
降级期间反而不写。所以这一刀不做。

**版本**:`bot.real.js` `057c5aad…` → `359160d3…`,三个机器人都重启了(12:56:50 / 12:57:10 / 12:57:31 JST)。

**实测**(改前 12:46:30~12:56:30,改后 12:57:40~13:07:40,各 10 分钟):
| | 改前 | 改后 |
|---|---|---|
| 日志里看得见的模型失败 | 0(不是没有,是看不见) | 小麦 6、麦娘 3、麦豆 6 |
| 主脑降级 | 0(触发不了) | 0(这段时间没超时) |
| 接小目标 / 达成 | 16 / 3 | 13 / 1 |
| 执行成功 / 失败 | 38 / 35 | 44 / 38 |
| 硬错 | 0 | 0 |

- ⚠️ 失败数要**按行里的累计数加,不能数行数**:`🧠✗` 30 秒只打一行,被压住的次数写在下一行的「上一行之后共 N 次」里。
  我最初数行数,把小麦报成 4 次(实际 6 次),平级会话复核时纠正。以后以每小时那行 `🧠📊` 为准。
- **看得见之后第一眼看到的**:麦豆的单步决策反复选「砍树」,可砍树根本不是它会的动作(被判「答不对」),10 分钟 6 次(平级会话逐行数的,我原写 5 次);
  小麦在不该合成的时候选「做木剑」。这些以前都是静默丢掉的。
- ⚠️ 两段窗口又是**不同的昼夜相位**(改前是黎明前后、改后是夜里),接活、达成的数都太小,**只能说「没看到坏处」**。
  这一刀本来也不改行为,对照主要是确认它没把什么弄坏。

**回滚**:换回 `057c5aad…` 那一版,三个服务各重启一次。

---

## 2026-09-18 🌾 机器人不再踩坏耕地 —— 12:45 JST 上线

**起因**:平级会话(fork 2)用 CoreProtect 只读查出,小麦 09-16 03:13 ~ 09-18 10:49 **踩坏耕地 30 次**。
每次都是同一秒三条记录:小麦弄坏耕地、小麦"放"耕地(被踩回泥土的那一下,CoreProtect 记成两条)、庄稼被弄坏。
这是**踩踏**的特征,不是收割。碰的是 Owner 的红线「不破坏别人的东西」。

**被踩的是谁的**(平级会话 fork 1 又直接核了一遍数据库;下面只写**能证明的**):
| | 玩家的东西 | 自然村庄的农田 |
|---|---|---|
| 耕地 30 格 | **16 格是 Owner 本人开垦的**(出生点旁那片菜地) | 14 格 |
| 连带毁掉的庄稼 16 棵 | **Owner 种的 7、howard_bear 种的 1** | 8 |

- **真正碰了红线的是左边那一列**:Owner 的菜地(耕地 16 格 + 庄稼 7 棵)和 howard_bear 的 1 棵。
- 右边那一片查不到开垦者和种植者。平级会话用游戏自带的 `/locate structure #minecraft:village` 核实过:
  那片中心离一个针叶林村庄 **4 格**;对照组 Owner 的菜地离同一个村庄 **419 格**。
  村庄农田是世界生成时自带的,村民农夫补种是实体行为,CoreProtect 都不记成玩家,所以查不到来源。
- 那个村子里有孩子们放过方块的记录(MC_wjljh、howard_bear),是孩子会去的地方;
  那里还有劫掠兽的记录,但记在小麦名下的那 8 条破坏确实是它。
- ⚠️ 我在代码注释里一开始照抄了「全是别人开垦的」「孩子们的庄稼 8 棵」,两句都不对,已按上表改正。
- 同期**麦娘踩坏自家田 12 次、麦豆 3 次**,是在毁自家的庄稼。这道护栏一并管住。

**为什么会踩**:耕地只在**落到**它上面那一下才会坏(跳下来、跑酷跳、边跑边跳落地),平着走上去不会坏。
而寻路库跑不成直线时会**边跑边跳**(pathfinder 2.4.5 `index.js:614` 的 `canSprintJump`),落点就是路径上的下一格。

**修法**:包住寻路的 `getNeighbors`(A* 只从这里拿「下一步能去哪」,`astar.js:85`):
**任何一步,只要落脚的那块地面是耕地,这一步就不存在。**
- 三份寻路配置都挂上了(机器人自己那份、砍树 collectBlock 那份、打架 pvp 那份);漏一份,砍树或打架时就还会踩。
- 麦娘种地本来就站在田边干活。只有「捡收割掉下来的东西」原来会走进田里,改成走到旁边 1 格(捡东西的范围够得着)。
- 附近有田时,每 10 分钟最多打一行「🌾 寻路绕开耕地:剪掉 N 条」,证明护栏真的在起作用。

**为什么没用更简单的写法**:平级会话最早建议用寻路库自带的 `exclusionAreasStep`「往下看一格」。
我在服务器上用**真的 pathfinder 2.4.5 A\* + 真的 Movements** 搭了个离线小世界比过(没连游戏服,也没碰在跑的机器人):

| 场景 | 不加护栏(对照) | 「往下看一格」 | 包 getNeighbors(上线的) |
|---|---|---|---|
| 横穿 3 格宽的菜地 | 踩 2 格(其中有跑酷跳) | **仍然跑酷跳到耕地上** | 绕过去,0 格 |
| 从 3 格高的台子下到菜地 | 落到耕地上 3 格 | **仍然落到耕地上** | 换个地方下,0 格 |
| 附近没有耕地 | 路长 10 | — | **路长 10、代价完全一样**(不影响平时走路) |
| 目标就在菜地正中间 | — | — | 找不到路(宁可走不到,也不踩) |
| 捡田边的东西(范围 1) | — | — | 能走到,不进田 |

原因:跑酷跳在代码里查的是**头那一格**,而落差 ≥2 走的是 `getLandingBlock`,都不经过「往下看一格」。

**挡不住的**:被怪物击退、被人推到田里。那不是它自己走上去的。

**版本**:`bot.real.js` `8d6008c2…` → `057c5aad…`,三个机器人都重启了(12:45:37 / 12:45:57 / 12:46:17 JST);
启动日志里每个机器人都有 3 行「🌾 已给「…」那份寻路挂上不踩耕地的护栏」。

**怎么验证**(用 CoreProtect 数,不用机器人自己的日志 —— 它自己不知道自己踩了):
| | 基线(全部历史,截至 12:46 JST) | 12:45 之后 |
|---|---|---|
| 小麦 | 30 次,最后一次 09-18 10:49 | 待查 |
| 麦娘 | 12 次(自家),最后一次 12:42 | 待查 |
| 麦豆 | 3 次(自家),最后一次 12:37 | 待查 |

查询脚本(只读)在服务器上:`sudo python3 /home/howard/farmland_check.py "2026-09-18 12:45"`。**过几个小时应该是 0**;不是 0 就按它给出的坐标和时间对日志。

**回滚**:把 `bot.real.js` 换回 `8d6008c2…` 那一版,三个服务各重启一次。(`bot.real.js.prev-safe` 只保留**最近一次**部署之前的那份,之后再部署就不是它了。)

---

## 2026-09-18 ✅ Owner 选了「彻底关掉」规划师 —— 12:07 JST 上线

> 这一条原来的标题是「⏸ 待 Owner 决定」,决定之前的讨论原样留在本条下半部分。
> 事情的来龙去脉(查老麦「浪费算力」,查出来的是规划师)在 [docs/27](../docs/27-the-loudest-is-not-the-costliest.md),平级会话写的。
> Owner 选的是「彻底关掉」,**没有**选推荐的「放慢到 60 秒」。

**改了什么**(`~/mcbot/bot.real.js`,三个机器人共用一份,改完三个都重启了):
- 新增开关 `PLAN_ENABLED`,**默认关**:env 里写了 `PLAN_ENABLED=1` 才启动规划师循环。三份 env 都没写,所以三个都是关的。
- 关着时启动日志**如实打一行**「规划师已关闭(PLAN_ENABLED≠1,Owner 2026-09-18 决定)……」,免得下一个人以为它坏了。
- 顺手加了 `PLAN_EVERY_SEC`:打开之后多久想一次,默认 60 秒,取值 1~86400。
- 关着时,给模型看的状态里「现在这一阶段要做的」固定是「(还没定,先按大目标来)」,这是原来就有的兜底文案。
  独立复核确认:选活的两个提示词(挑脚本 / 单步决策)里没有一句让模型服从这一行,脚本判据也不读它。

| 版本 | md5 | 上线 (JST) | 内容 |
|---|---|---|---|
| 关之前 | `3e0bee5e…` | — | 规划师每秒一轮 |
| v1 | `3c24d7f2…` | 12:07:49 | 开关 + 启动日志 |
| v2 | `8d6008c2…` | 12:21:42 | 只改「打开时」的等待:① 加 86400 秒上限(setTimeout 超过约 24.8 天会溢出,Node 悄悄改成 1 毫秒,填个大数反而变连珠炮);② 失败退避不许比正常间隔还快(默认正常 60 秒、退避封顶 30 秒,后端坏了反而敲得更勤)。**关着时这段代码不执行。** 两处都是独立复核查出来的 |

**实测:模型网关的调用数**(网关 `/health` 累计值,四台机器求和;窗口起止都打印核对过,四次读数都存了档)

| 窗口 (JST) | 版本 | 调用数 | 说明 |
|---|---|---|---|
| 11:57:35 → 12:07:37 | 关之前 | **212** | M1 +71、iMac1 +72、iMac2 +69 |
| 12:09:35 → 12:19:38 | v1 | **52** | M1 +18、iMac1 +17、iMac2 +17 |
| 12:19:38 → 12:36:07(16.5 分钟) | v1→v2 | 47 | 补的一段,换算约 28 次/10 分钟;中间还重启过一次 |

- 5060 那台四次读数都是 2077,**整段时间都没接活**(游戏本那台的模型是关着的),所有请求都落在三台 8GB 的 Mac 上。
- 两个窗口里服务器上**只有三个机器人在线**:10:37 开服以来没有真人进出,也没有聊天。所以老麦是 0,不影响对比。

**这组数能说明什么、不能说明什么**(三个对抗复核员挑过毛病,下面是它们的结论):
- ✅ **规划师确实关掉了。** 最硬的证据是代码:没打开就根本不启动,日志里有启动那一行作旁证。
  光靠「日志里没有规划师的行」**不够**:它以前没被采纳的调用从来不写日志,开着的时候也常常一行都没有。
- ⚠️ **「少掉的约 160 次都是规划师的」是推算,不是点数。** 以前没人数过规划师到底调了多少次。
  按代码推算,每轮是「模型回复约 9~10 秒 + 等 1 秒」,三个机器人合计每 10 分钟约 150~180 次,和少掉的数对得上。
- ⚠️ **混杂因素**:改前那段是游戏里的**夜里**,改后那段是**白天**;中间三个机器人还一起重启了一次。
  补测的那 16.5 分钟里又包含了一段夜里,仍然只有约 28 次/10 分钟,所以昼夜解释不了大头。
  但**每小时省多少**只能给个量级(约 900~1000 次),给不出精确数。
- ⚠️ **「干活没变差」没有被证明,只能说「没看到坏处」。** 表面数字:接小目标 13→15、达成 7→8、执行成功 43→51、失败 25→19、硬错 0→0。
  但「成功」里有很多原地空转(夜里「已经睡着了,等天亮」18 次、白天小麦「附近没动物」21 次),扣掉之后是 24 对 25,基本持平。
  每个窗口每个机器人只有 1~9 个小目标,样本太小。反过来从代码看,规划师本来就只往提示词里加一行字,对行为的影响本来就小。
- ❌ **不能说「机器人想得更快了」**:改前改后,模型回一次都大约 10 秒。
- ⚠️ 日志里的「大脑决定」行数**不是模型调用次数**:它把硬规则派的活、脚本步骤都算进去了。小麦的 138→54 不要当调用数引用。

**回滚 / 重新打开**:在要打开的那个机器人的 env(root 的 `/etc/mcbot.env` / `/etc/mcwife.env` / `/etc/mcchild.env`)里
加 `PLAN_ENABLED=1`,重启那一个服务。想完全恢复关之前「每秒一次」,再加 `PLAN_EVERY_SEC=1`。

**复核时顺带查出来的老问题(和这次改动无关,改之前的版本里一模一样,都还没改)**:
1. 🔴 **模型答砸了是完全静默的**:`brainTick` 里有一句 `if (!action) { return }`,排在所有失败记账**之前**。
   脚本模式下,模型超时或乱答都直接跳过,一个字不记。所以:
   - 日志里「0 个错误」**不代表模型没出错**;
   - 「主脑连续 2 次没反应 → 切备用入口」那套保护**永远不会触发**;
   - 「连着答不出 → 代码替它选」那条兜底也是死代码。
2. 🔴 **「没活可干」时是按时间问模型**:一个脚本都开不了工时退回单步决策,而这条路**没有退避**,
   按代码,小麦的大脑循环最快约 2.5 秒一轮、每轮都会问一次;但 ① 上线后第一个小时的 `🧠📊` 汇总里,
   小麦单步决策实际只有 **83 次**(模型平均 6.4 秒回一次),远少于「2.5 秒一次」算出来的 1440 次 ——
   **这个说法和实测对不上,原因还没核清**(平级会话指出),不要引用。可以确定的是:它是按时间在问,而不是只在选择点问。
   这其实就是 Owner 否掉的「定时问模型」,只是换了个地方。
3. `brain.js` 的 `callModel` 收了 `numPredict` 参数却从来没用,所有调用的输出上限都写死在 200。
   今天没影响:真正会走到的调用要的都不到 200;要 300/320 的规划师和 makeTask 现在都走不到。
   **不改**:照参数改反而会把正在用的挑脚本 / 聊天 / 设计房子压到 120/90/80,那是行为变化。
4. 小麦「打猎」没找到动物也记成**成功**(「附近没动物……我已经站在记得的「动物」里了」,10 分钟 21 次),
   同时饿着、身上没吃的,血最低掉到 3.4;而它自己地图上记着南边约 7 格有鸡。属于 Owner 说「先放着」的吃饭问题,但记账是错的。
5. **重启本身有风险**:重启会清掉内存里的保护状态(「这段时间先别回家」、脚本冷却等)。
   麦娘重启后直接回了家,血掉到 2.1。**以后能少重启就少重启**,换代码时要把这点算进去。

---

### 决定之前的记录(原「⏸ 待 Owner 决定:规划师还要不要一直跑」)

在现在这套「职业 + 脚本」的架构下,**规划师的产出已经基本没人用了**,但它还在一直调模型:
- 按代码,规划师循环**每 1 秒一轮**,每轮调一次模型;三个机器人各跑一个。
- 结果**大多直接扔掉**(代码原话:「没过期就丢掉这次结果」)。
- 最近一小时,小麦和麦娘的规划师**一次新目标都没被采纳**;
  刚上线的麦豆采纳了 4 次,其中还有它根本做不了的"去林子砍树"。

**为什么没有直接关**:这个"让显卡一直忙着"的设计,是按 Owner 早先的
「GPU 应该是连续不断地在思考和执行才对」做的,和今天的「纯浪费算力」方向相反。
两句话都是 Owner 说的,取舍是他的事。

> 🔴 **更正**:我这里原来写的是「关掉只要一个开关、不用改代码」。**不对。**
> 现在的代码里**没有**规划师的开关 —— 它只有"只启动一次"的保护;
> 唯一能挡住它的是把整个大脑关掉,那显然不是要的效果。
> 环境变量里和规划师有关的只有地址、模型、超时和"计划保鲜多久",**没有一个能让它停下或放慢**。
> (把地址指向一个不存在的地方也不算开关:那会进退避,最长 30 秒还敲一次门,每 60 秒写一行告警。)
> **所以不管关掉还是放慢,都要改一行代码并重启三个机器人。** 我是把"我可以加一个开关"说成了"已经有一个开关"。
> 这条是平级会话核到代码层之后指出来的。

**两个选项**(都要改一行代码):
| 选项 | 效果 |
|---|---|
| **放慢到每 60 秒一轮**(平级会话的建议) | 计划本来就只在过期(60 秒)时才被采用一次,所以**每秒问和每 60 秒问产出的价值一样**,调用少 95% 以上。两句 Owner 原话都不违背 —— 规划师照样在想,只是不再做被扔掉的那 59 次 |
| 整个关掉 | 调用降到 0;脚本层不依赖它,麦豆也不会再被派做不了的活 |

另外一处更准确的说法(也是平级会话更正的):规划师**是**拿到了各职业的动作清单的,
是**小模型不遵守** —— 麦豆照样被派了「去林子砍树」。
**把约束写进提示词 ≠ 模型会遵守**,和 [11 篇](11-ai-player-brain.md) 第四节
"模型只挑脚本、不拼动作"是同一个道理。

---

## 2026-09-18 网页发言:记住的密码改成闲置 15 分钟失效

> 这条由负责网页聊天的平级会话(fork 2)排查和部署,我代为记录。

**起因**:Owner 看到网页聊天里 07:44~08:02 有 6 条署名是他网页发言身份的话,说不是他发的。
其中一条是「能不能把地面上的垃圾拆了,回头我给你们发半组钻石」——
**孩子们会当成管理员的承诺。**

**查法**:发言日志(`/var/lib/mc-chatweb/send.log`)记着时间 / 名字 / 来源 / 内容,
nginx 的访问日志记着浏览器指纹。对下来:
- 这个身份的全部 17 条发言,来自**同一个出口、同一个浏览器**(Mac 上的 Chrome 152),
  其中就包括 Owner 自己凌晨的「发言测试」和「晚安」;
- 从 Owner 笔记本发出的请求走的也是这个出口 → 就是他家的网络;
- 该出口 03:00~10:00 每小时 3800~7600 次请求 → 一个**整夜开着的地图网页**在轮询;
- Owner 笔记本是 Chrome 153(而且前一天就是)→ 发言的是**家里另一台 Mac**。

**结论:不是密码泄露。** 是家里另一台 Mac 上开着的地图网页**永久记住了密码**,
早上有人坐下来打字。

**根因在设计**:网页把密码存在浏览器里,而且永久有效。

**改了什么**:`/var/lib/mc-chatweb/chat-overlay.js`
- 记住的密码在**最后一次成功发言后闲置 15 分钟**即失效;
- 存储键从 `mcchat.pw` 换成 `mcchat.cred`,旧版永久存的那个**读到即删**;
- 网页开着时也按时锁定,并把藏在输入框和内存里的那份一起清掉。

浏览器里验过四种情况。

**注意**:已经开着的旧标签页要**刷新一次**才用上新规则;最快的止血是在后台换密码。
**回滚**:用 `/var/lib/mc-chatweb/chat-overlay.js.bak-idle-*` 覆盖回去。

**事后核对:6 条发言时服务器上没有任何真人在线,没有孩子看到。**
方法是从当天 05:01 那次重启起逐条回放 `joined` / `left`,求每条消息那一刻的在线名单
(发言日志和服务器日志都是日本时间,不用换算):

| 时间(JST) | 在线的真人 | 在线的 AI |
|---|---|---|
| 07:44:33 | 没有 | 麦娘、小麦 |
| 07:58:17(许诺钻石那句) | 没有 | 麦娘、小麦 |
| 08:02:08 | 没有 | 麦娘、小麦 |

07:30~08:30 之间真人一个都没进出过。所以那句「你们」只可能是说给两个 AI 玩家听的 ——
有人在地图上看见小麦在地上堆了一片东西,叫它俩收拾。游戏里也没有任何人接话。
**不需要去游戏里向孩子们解释。**

> **这条的通则**:一个"记住我"的功能,等于把身份交给了**那台设备旁边的任何人**。
> 在一个家里有孩子、电脑共用的场景下,"记住密码"的有效期必须按**闲置**算,
> 而不是按"直到有人手动退出"算 —— 因为不会有人记得退出。
> 另外,排查"是不是泄露了"时,**来源 + 浏览器指纹**比猜测快得多:
> 一眼就能分清"外人拿到了密码"和"家里另一台设备开着"。

---

## 2026-09-18 🔴🔴 第三版,也是对的那一版:真因就是屋顶那块土,而我把时区搞错了

**这件事我错了两轮、写进公开文档两次、还让平级会话照错机制写了一篇 docs。
下面是闭合的时间线。原来那两条更正都保留不删 —— 它们本身就是这条教训的一部分。**

### 闭合的时间线(全部换算成 UTC,一个变量、一次去除、零反例)

```
15:17:21.999  🧱 (-18,65,160) 放上 dirt          ← 小麦铺屋顶,土盖到了自己箱子正上方
15:28:37.660  麦娘第一次「打不开箱子」             ← 11 分钟后
   ……         连续 28 次
16:06:59.252  最后一次失败
16:07:38      [Rcon] Changed the block at -18,65,160   ← 我把它清成空气
16:09:44.862  🔬 开箱子成功 + 成功:往箱子里存了 27 个木料   ← 2 分钟后就好了
16:35:42      [GriefPrevention] Deleted all of XiaoMai's claims.  ← 晚了 26 分钟,和箱子无关
```

**原版规则:箱子正上方有不透明方块就打不开。真因就是这个,没有别的。**
而这个真因**我当天就找到并修掉了** —— 我只是不相信自己修好了。

### 我为什么不相信

**① 时区。** 服务器日志是 **JST**,机器人日志是 **UTC**,差 9 小时。
我拿服务器日志里的 `01:07:38`(清方块)去比机器人日志里的 `16:01~16:06`(失败),
看着像「清完还在失败」。换算过来 `01:07:38 JST = 16:07:38 UTC`,**在所有失败之后**。

> **我们的记忆里本来就有这一条**(「机器人日志 UTC、服务器日志 JST,务必换算」)。
> 有笔记没用上,和没有笔记的后果一样 —— 而且更难发现,因为我以为自己知道这件事。
> **可执行的版本**:两个来源的时间戳放在一起比之前,**先把其中一个换算并写下来**。
> 光"知道有 9 小时差"不够,必须真的动笔换算。

**② grep 只匹配了新日志格式。** 我查「第一次成功」用的是 `🔬 开了` ——
那是我**后来**才加进代码的字符串。真正的第一次成功在 `16:09:44`,
用的是旧格式 `🔬 开箱子`。于是我把恢复时间读成了 17:24,晚了 75 分钟。

> **同族**:用一个"我以为一直存在"的标记去检索历史,会把改动之前的那一段整段漏掉。
> 查历史事件时,要先确认**那个字符串在当时的代码里存不存在**。

### 而那个「干净的对照组」根本不是对照组

我当时的推理是:同一口箱子,**小麦(所有者)15:16 能开、麦娘(被 `/trust` 的)15:28 不能开**
→ 所以是权限问题。

**两次观察隔 12 分钟,而那块土正好落在中间(15:17:21)。**

> **「A 能、B 不能」只有在【同时】观察时,才指向 A 和 B 的差别。**
> 不同时刻的两次观察,指向的是**这期间有什么变了**。
> 正确的下一问是:**这两次之间,有没有人动过什么?** —— 而机器人自己就动了。

这条是平级会话点出来的,三个会话此前都把它读成了身份问题。

### 因此作废的两条结论

- ❌ 「一次被掐掉的合成会泄漏窗口,导致之后所有开箱子失败」——
  **这次没有僵尸参与**。她整个日志只被封顶掐过 1 次(15:58),比第一次失败晚半小时;
  我加的「关泄漏窗口」日志 **0 次命中**。机制在库里**确实可能**,但这次**没有发生**。
- ❌ 「GP 的 `Builders` 不含开容器权限」—— **没有任何证据支持**,请不要再往下传。
  箱子在删领地之前 26 分钟就已经恢复正常了。

### 那四处窗口修复怎么算(理由要写对)

**留着,但理由必须写准**:留它们是因为**「库里确实可能发生」**,
不是因为**「已经发生过」**。代码注释里已经改成这么写了 ——
> **把理由写清是为了下一个人:别看到"从没命中过"就当过度设计删掉。**

### 顺带按平级会话的建议做了一件对的事

他问:「这次是箱子,下次可能是熔炉 —— 为什么不把清单一次列全?」对。
所以现在有一份 `NEEDS_HEADROOM_RE`,一次列全:
**箱子 / 陷阱箱 / 末影箱 / 潜影盒**。

- ⚠️ **桶(barrel)不在清单里** —— 桶头顶有方块照样能开。
  **以后要放长期容器,优先考虑桶**,这比任何护栏都硬。
- ⚠️ 工作台 / 熔炉 / 铁砧 / 酿造台**不需要**头顶留空,**故意不列** ——
  把不需要的东西列进禁止清单,会平白挡掉能放的格子。

而且房子图纸的剔除逻辑也从「剔掉我记得的那三个箱子坐标」改成了
**「扫一遍真实方块,下面是这类容器就剔掉」** ——
前者隐含「我知道所有箱子在哪」这个假设,而孩子可能在房子占地里放过箱子。

---

## 2026-09-18 🔴 更正上一条:那个「被掐掉的合成泄漏窗口」的机制,被日志推翻了

**下面那一条我写错了,而且写得很自信。这里是更正,原条目保留不删 ——
删掉会让后来的人看不到"一个听起来很合理的机制怎么被证伪的"。**

### 证伪它的三条证据

| 我当时说的 | 日志实情 |
|---|---|
| 被 30 秒封顶掐掉的 `craft` 留下了没关的窗口 | 她整个日志里**只有 1 次**封顶掐掉,而且是 **15:58 的 `store_items`** —— 比第一次「打不开箱子」(15:28)**晚了半小时** |
| 我加的「开窗前关掉泄漏的旧窗口」修好了它 | 那行日志 **0 次命中**(代码在、正对照过)—— **从来就没有过一个开着的旧窗口** |
| 第一次成功是因为我的窗口修复上线了 | 时间对不上(见下) |

### 时间线指向的是别的东西

```
15:28 → 16:06 UTC   她连续 28 次「打不开箱子」
16:35:42 UTC        平级会话执行 Owner 的指令,删掉了小麦的全部领地
                    (服务端原话 [GriefPrevention] Deleted all of XiaoMai's claims.)
17:24 UTC           她第一次成功开箱子
```

**删领地这一步,正好夹在最后一次失败和第一次成功之间。**

### 而决定性的对照,我当时握在手里却读反了

同一口箱子、同一时段:
- **小麦(领地的所有者)** 15:16 还成功「往箱子里存了 19 个木料」
- **麦娘(被 `/trust` 写进 `Builders` 的人)** 一次都开不了

我用这个对照**排除**了「箱子坏了/被封住了/离太远」—— 那部分是对的。
但我**没有**用它推出正确的那一半:**能不能开取决于"是谁"** —— 也就是权限。

**为什么我排除了权限**:因为「GP 拒绝时会在聊天里说话」,而她没收到任何拒绝消息。
**这一步是错的** —— 我把 GP **拒绝建造**时会发消息的行为,
**推广到了它拒绝开容器的时候**。前者我有实证(955 条 🚫),后者我一条证据都没有。

> **教训**:一个"它平时会说话"的观察,不能当成"它这次不说话就说明不是它"。
> 沉默不是排除证据,除非你**专门验过它在这种情况下也会说话**。

### 现在的结论(以及仍然没查清的部分)

**有证据的**:领地存在时,被 `/trust` 的成员开不了这口箱子;领地删掉之后能开了。
**没查清的**:为什么 GP 的 `Builders` 没能覆盖开容器 ——
可能是 GP 的权限缓存没刷新(`/trust` 是在她已经在线之后加的),
也可能这个版本/配置下建造信任不含容器,需要单独 `/containertrust`。
**这一条现在没法复验了** —— 领地已经删了,而 Owner 说暂时不要再圈。
等 Owner 重新批准圈地的时候,应该**先只加 `/containertrust` 试一次**,那会是干净的一枪。

### 那四处窗口修复怎么算

它们**不是这次故障的解药**,但**每一处单独看都仍然是对的**,所以留着:
开新窗口前先关掉可能残留的旧窗口、永远不要让库默认的 20 秒超时吃掉动作预算的三分之二、
失败文案里带上"现在是否还有窗口开着"。
**区别在于:它们现在的定位是"防患",不是"已验证修好了什么"。** 按这个目录的规矩标清。

### 顺带更正一处源码结论

我在 12 篇里写过「`openChest` 在 mineflayer 层面没有超时」—— **这句不对**。
`lib/plugins/inventory.js` 里那个 `once` **不是 Node 的 `events.once`**,
而是 `lib/promise_utils.js` 自己的版本,**默认就带 20000ms 超时**,
那句 `Event windowOpen did not fire within timeout of 20000ms` 就是库自己抛的。

**这个错误的来历值得记**:我派去读源码的调研员**明确写了**
「`promise_utils.js` 不在快照里,所以 `once()` 的确切实现读不到」——
**而我把"读不到"写成了"没有超时"。** 调研员诚实地标了不确定,
是我在综合的时候把它丢掉了。
> **别人如实标出的"我不知道",到了你笔下不该变成"没有"。**

---

## 2026-09-18 一次被掐掉的合成,会让【之后所有开箱子】永久失败到重启为止

**症状**:麦娘连着 10 次
`失败:打不开箱子(Event windowOpen did not fire within timeout of 20)`,
而同一口箱子小麦一直开得好好的(15:16 还成功存了 19 块)。

**排除掉的(每一条都查到底了)**:

| 猜测 | 怎么排除的 |
|---|---|
| GP 领地没授权 | `116.yml` 的 `Builders` 里就是她的离线 UUID,**逐位核对过** |
| 离线 UUID 算错了 | 自己用 `md5("OfflinePlayer:MaiNiang")` 算了一遍,和文件里一模一样 |
| 屋顶把箱子封住了 | **这是真缺陷**(见下条),但修完她照样失败 |
| 离得太远 | 有一次她就站在箱子那一格上(距离 0.0),照样失败 |
| nLogin 没登录 / GrimAC 拦了 | 服务端日志里一条拒绝记录都没有。**GP 拦的时候是会说话的** —— 她收到的 3 条 🚫 都是别的时间、别的地点 |

**真因是一条链**:

1. 动作封顶用 `Promise.race`,而 **`Promise.race` 不会取消输的那一边**;
2. 一次 `craft` 被 30 秒封顶掐掉之后变成**僵尸继续跑** ——
   它**已经把工作台窗口开出来了**,却再也走不到自己的 `closeWindow`;
3. mineflayer 的 `openChest` 内部是 `await once(bot, 'windowOpen')`,而
   **已经有窗口开着时服务器不会再发这个事件** → 死等 20 秒。

也就是说:**一次被掐掉的合成,会让之后所有开箱子/开工作台永久失败,直到进程重启。**

> 最值得记的是:这条链的**上半截我自己文档里早就写了**
> (「僵尸续跑还是会把合成窗口开出来」),但我只修了预算,没追问
> **「那个已经开出来的窗口后来怎么了」**。
> 教训:**一个副作用要一路追到底 —— 追到它被谁收拾掉,或者确认没人收拾。**

**修了四处**(任一处单独都不够):
- 开箱子前、开工作台前,各先关一次可能泄漏的旧窗口(**不静默**,每次留一行日志);
- 反射层(每秒一次)兜底:**没有动作在跑却有窗口开着超过 3 秒**,在这套代码里必然是泄漏,关掉
  —— 光在"开窗之前关一下"不够,泄漏存在期间**任何**动作的开窗都会失败,包括以后新写的;
- 开箱子的等待从 mineflayer 默认的 **20 秒压到 6 秒**:动作预算总共只有 30 秒,
  光等一个开不了的窗口就吃掉三分之二,连"如实汇报"的时间都不够;
- 失败文案里带上**当前是否还有窗口开着**(id + type)——
  这个故障会自我掩盖成"网络没回包",必须让它自己说出来。

**⚠️ 还没验到**:修完之后她身上恰好没有可存的东西,所以一次都没再试过。
逻辑上必然对(关掉旧窗口永远不会错),但**没有"她真的存进去了"这条证据**。
按这个目录的规矩:没验到就写没验到。

---

## 2026-09-18 屋顶把家里唯一那口箱子封死了(我自己造的)

给房子加屋顶之后,机器人把土块铺在了 `(-18,65,160)` —— 正好是箱子
`(-18,64,160)` 的头顶。**MC 的规则是箱子上方有实心方块就不能打开。**
那块土后来还长成了草方块,所以从材料上都看不出是它自己铺的。

**修了两处,少一处都不行**:
- `placeAt`(所有放方块动作的总闸)加一道:**下面是箱子就拒绝放**,理由写清;
- **图纸本身也要认这条规则**:把箱子头顶那一格从房子图纸里剔掉。
  只加总闸不够 —— 那一格还留在图纸里的话 `todo` 永远清不空,
  房子**永远判不了完工**,盖房脚本会一直失败、一直进冷却。

> 判据放在总闸上而不是放在盖房动作里:**所有**会放方块的动作都可能犯这个错
> (盖房、挡箭、以后的耕地播种),而这个目录已经三次栽在「同一个判据写了两份」上。

---

## 2026-09-18 网页发言开通(后台配密码,已实测)

**状态**:已开启。后台「网页发言」页配了 1 个密码。

**实测(走公网 https 那条真实路径,不是内部捷径)**:密码不对→挡住;密码对→进了游戏,
显示 `[网页] 名字: 内容`;2 秒内连发第二条→限速挡住;空内容→挡住;
点名 AI 村民→10 秒后它回话,回话又显示回网页;RCON 回执非空(证明命令真进了服务器)。

**AI 村民能听见网页的话了,而且没改 McAI 一行代码** ——
用它自带的 `/mcai ask` 从 RCON 调。触发规则照搬 McAI 自己的 config
(`on-name: true` + `cooldown-seconds: 6`),AI 名字不写死、从它自己的发言里学,
后台改名会自动跟上。

**修的两个问题**:
- 网页输入框会触发地图快捷键(BlueMap 在 window/document 上听 WASD)→
  改成在**捕获阶段**挂 window 拦截;
- 后台保存报 `permission denied` —— 配置文件当初是用 root 建的(`root:mcpanel 0640`),
  而后台以 `mcpanel` 身份跑、组只有读权限。用 `--reference` 对齐成 0660。

**🔴 通则:`/var/lib/mc-panel/` 下任何给后台写的文件,都不能用 root 去建。**
后台是 `mcpanel` 身份;root 建出来的它打不开,而错误**只在页面上弹一句
`permission denied`,journal 里什么都没有**。新建文件一律
`chown --reference` / `chmod --reference` 抄旁边那个已知能用的。
另外那个**目录**对 mcpanel 不可写,所以只能原地改写、不能 temp+rename。

**🔴 一个验错了的方法,别学**:用
`nsenter -t $PID -m -- test -w <文件>` 验"后台写不写得动",结果"✅ 可写" ——
**`-m` 只进挂载命名空间、没换身份,那句其实是以 root 跑的**,而 root 写得动任何东西。
正确的是 `sudo -u mcpanel test -w <文件>`。
反向的坑也有:`sudo -u mcchat test -r` 说读不到,但服务其实读得到 ——
因为那个组是 systemd 用 `SupplementaryGroups=` 给**进程**的,不在**用户**的组里。
> **用户权限 ≠ 进程权限,验的时候要选对哪一个。**

**怎么关**:后台「网页发言」页去掉「允许网页发言」的勾再保存,立刻生效。

---

## 2026-09-18 AI 玩家改成「职业 + 脚本」架构(Owner 定的规格)

**这是这个目录里最重要的一条。** 在此之前 AI 玩家的循环是:
**每隔 2.5 秒问模型「从这 18 件事里挑一件」→ 做最多 30 秒 → 扔掉 → 再问一次。**

Owner 2026-09-17/18 原话(规格就是这几句):
> 「现在ai玩家的逻辑是不是在一堆随机的预设动作中 让ai选一个? 是不是要加一些上下文
> 前置条件啊? 比如主线目标,当前的小目标, 紧急事件 等等, 不能让ai玩家一直打转做一些
> 跟目标无关的事情 … 因为紧急事件被打断的任务要恢复起来继续完成. 另外在没有发生紧急
> 情况和被玩家触发的时候 不要出现让ai去选择下一步的动作, ai的介入应该是出现选择的
> 时候 而不是个定时器」
> 「我同意每隔一段时间发送目前ai玩家的情况给到ai, 但是如果情况没啥问题就继续上一个
> 任务 直到完成呀」
> 「这里特别关键的是一定要给到ai模型 前面完成的脚本是那些 包括顺序 要带上目标 以及各类情况」
> 「大目标不一定就只有一个 可以有很多 发散思维吧」
> 「这个逻辑是否可以作为可视化的后台进行管理 也是一个值得考虑的问题」

### 为什么必须改(量出来的)

查了 2026-09-17 09:00~13:11(**UTC**,即 JST 18:00~22:11)的 **1422 次决策**:

| 量出来的 | 值 |
|---|---|
| 相邻两次 `explore` 同向 | **44%**(乱猜是 25%)—— 它每次都在"重新想" |
| 离家距离 中位 / p90 / 最远 | 76 / 165 / **259 格** |
| 待在离家 60 格以外的时刻 | **64%**,而家当全在家 |
| `gather_wood` 被选中 | **517 次 = 落地决策的 36%**;另有 159 次想砍树但被挡回。算上被挡回的,模型所有回答里想砍树 676/1604 ≈ **42%**,绝大多数注定失败 |

> 🔴 **更正(2026-09-18)**:原表写「`gather_wood` 被选中 681 次 = 全部决策的 48%」,平级会话重数日志后指出两处错:
> ① 681 是把被挡回的也算进了分子,却没算进分母;② 窗口的时区没写,实际是 UTC。按上表的两种口径改正
> (两种口径回答的是不同的问题:实际去做的里有多少是砍树 / 模型心里有多想砍树)。

决定性的一条:13:11 它在 `(-16,318)`,自己的地图上写着「能砍的林子=**北边** 131 格」——
那片林子在 z≈187,**离家只有 29 格**。**林子一直在家门口,是它自己往南飘了 160 格。**

### 改成什么

```
大目标(每个职业好几个,带优先级)
  └── 小目标 = 脚本(有名字、有开工条件、有完成条件、有步骤)
        └── 步骤 = 动作(可带「跳过条件」)
紧急事件 → 抢占,但【不动脚本的进度指针】→ 处理完自然接上
```

**模型只在这几种时候介入 —— 这就是 Owner 说的"出现选择的时候":**
① 手上没活(刚启动 / 上一件达成了 / 走不通放下了);
② 候选**有多个**时挑一个(只有一个候选时按优先级直接开工,**一次模型调用都不花**);
③ 玩家说话(另一条通道)。

### 三条最关键的设计决定,每条都有实测支撑

**① 「做完没做完」必须是代码判据,不能问模型。**
第一版我做成"每隔 60 秒问模型一句『当前任务还合适吗』"。实测 15:01~15:06:

```
🎯 例行过一眼:接着干「砍树拿木头」—— 目标已完成，存木头已超64块
🎯 例行过一眼:接着干「砍树拿木头」—— 目标已完成，木头已存入
……连着 6 次……
🎯 放下:干了 364 秒还没完,超过 360 秒上限
```

**模型一边说「目标已完成」,一边回答「继续」** —— 白烧 364 秒,最后靠超时才放下。
现在每个脚本自带 `完成条件`,`背包木头 >= 8` 这一行代码永远不会说谎。

**② 「能不能开工」也必须是代码判据。**
麦娘连着 3 次被派「去林子」,而她的地图里一个林子都没有,每次都以
「我还不记得任何『林子』」告终。现在前提不满足的脚本**不进候选名单**。

**③ 模型只【挑脚本】,不【拼动作】。**
让模型自己拼动作序列这一版实测过:她每一份计划都有 1~2 步被丢掉
(「不是我的活」/「我还不记得这个地方」)。改成给它几个**有名字的小目标**让它挑一个,
**模型不可能挑出做不到的事,因为做不到的根本不在名单上。**

### 履历 + 冷却(Owner 说"特别关键"的那条)

实测 15:21~15:22 的死循环:

```
15:21:50 接了「天黑了回家睡觉」→ 15:21:55 第 1 步失败 3 次,放下
15:22:02 又接了「天黑了回家睡觉」→ 15:22:07 又放下
……每 13 秒一轮,一直到天亮
```

放下了却马上挑回来,因为**没有任何东西记得它刚在这件事上栽过**。补了两样:
- **履历**(`history.json`,存盘):按顺序记"做过哪些小目标、成没成、为什么",
  每个选择点都原样喂给模型;
- **冷却**:刚栽过的脚本冷 2 分钟(超时的冷 3 分钟)不进候选,但会在
  「最近栽过还在冷却」里如实告诉模型 —— 免得它以为这件事没人试过。

选择点喂给模型的完整上下文(Owner:「甚至是箱子里的物品 以及 家庭角色情况」):
我的大目标 / 候选小目标 / **做过的小目标按顺序** / 最近栽过还在冷却 /
**家里的箱子里有什么** / **家里人(谁在线、谁被授权)** / 身上有什么 / 处境。

> 箱子内容只有开着箱子那一刻读得到。所以存东西时顺手记一份摘要进 `family.json` ——
> 这样麦娘不用开箱子也知道家里有没有种子。

### 后台可视化:做成了数据驱动

Owner 问能不能在后台管理。所以分两层:

| 层 | 内容 | 谁能改 |
|---|---|---|
| 代码 | 判据(34 个,如 `有能砍的树`/`背包木头`/`熟了的庄稼`)、动作 | 只能改代码 |
| 数据 | `scripts.json`:大目标表 + 21 个脚本(名字/职业/开工条件/完成条件/步骤/优先级/开关) | **后台网页可改,热加载** |

后台能做:开关某个脚本、调优先级、把脚本分给别的职业、改大目标文字、用判据菜单拼新脚本。
加**新判据**才需要改代码。(页面本身在 `admin-panel` 的 Go 源码里,那边有别人在动,要先对一下。)

### 现在的脚本表

| 职业 | 大目标 | 脚本 |
|---|---|---|
| **丈夫** 小麦 | 活着 / 家和家人安全 / 房子像个家 / 手上有家伙 / 不缺木料 | 砍树拿木料、把木料收进箱子、造工作台和箱子、把房子盖完、圈地、给家人授权、天黑睡觉、做一把剑、赶走家门口的怪 |
| **妻子** 麦娘 | 活着 / 全家有饭吃 / 有一块自己的地 / 家当都在箱子里 | 把家当收进箱子、把原木做成木板、从箱子里拿种子、做锄头、耕地、播种、收割、做面包、粮食进粮箱 |
| **儿子** 麦豆 | 别死别走丢 / 学会爸爸会的事 | 天黑或受伤回家、在家附近玩、跟着爸爸去林子 |

### 一个我自己写出来的 bug,记一笔

`开工条件` 我定义成「任一成立(OR)」,然后把打怪脚本写成
`开工条件:[8格内有怪, 血量≥14]` —— 按 OR 解释就是**残血 3 点也会去打怪**。
它不会报错,只会让它死得更快。所以另立一个 `还要满足`(全部成立),安全门槛放那儿。
顺手用同一个字段给睡觉脚本补上了「必须附近有床」—— 那正是上面那个死循环的直接原因。

### 已验到的

```
🧊 「build_house」冷却 120 秒,期间不再挑它          ← 不再每 13 秒挑回同一个
🎯 一轮走完还没达成(背包木头=4 ≤ 1)—— 从第 1 步再来一轮(第 2 轮)  ← 真的把一件事做完
🎯 现在一个小目标都开不了工(…已经达成:有箱子=是;冷却中:…)     ← 如实说明为什么没得挑
🎯 接了个小目标:「砍树拿木料」…(只有这一个候选,没问模型)       ← 没有选择就不惊动模型
history.json:[{"id":"chop_wood","ok":false,"why":"第 2 步…失败 3 次"},…]  ← 按顺序存盘
成功:做出了 1 个 wooden_hoe                                  ← 麦娘自己做出了锄头
```
两个进程各自 **0 个 TypeError / ReferenceError**。

### 还没解决的(如实写清)

- **屋顶停在「dirt 用完了,还差 11 块」12 次**:房子的材料是 `dirt`,而它**没有挖土的能力**,
  材料拿不到。脚本的开工条件查的是"任意一种盖房材料 ≥ 8",和 `build_house` 实际要的
  那一种不是同一个判据 —— 又是一次"两份判据"。要么让屋顶换材料,要么给它挖土的能力。
- **砍树被别人的领地挡住** 7 次(「附近有 30 根原木,但没有一根是我能砍的」)。
- **麦娘开不了家里的箱子** 4 次(`windowOpen did not fire`),虽然 GP 已经授权给她了 ——
  原因还没查清。
- **种地的动作还没实现**(`take_from_chest` / `till_soil` / `plant_seeds` / `harvest_crop` /
  `store_crop`)。脚本表里已经写好了,引擎会如实跳过并说明「不会 xxx」。

**回滚**:`TASK_MODE=0` 写进 `/etc/mcbot.env` 重启即可退回旧的单步决策,不用回滚代码。

---

## 2026-09-18 一条通则:生成器会整份重写的文件,手工改动等于临时改动

**起因**:资源世界的入口按钮**第二次**消失,孩子反馈「按钮不见了」。

真因不是菜单本身出错。`gen_shop.py` 会**整份重写** `DeluxeMenus/gui_menus/main.yml`
和 `config.yml` 的 `gui_menus:` 注册块。而资源世界的按钮和注册是**手工加上去的** ——
所以有人在后台点了一次「保存商店物价」,两处一起没了。
同一次还带走了 `strix`「世界传送」按钮和它的注册。

```
dm reload: 23 menus → 补 resworld 后 24 → 补 strix 后 25
```
`25` 这个数字本身就是证据:那两个菜单在事故前**压根没被加载过**。

**最毒的地方**:触发它的动作(改商店物价)和坏掉的东西(资源世界入口)看起来毫无关系,
所以第一反应绝不会是"我刚才点了保存"。

**通则**:
> 凡是 `gen_*.py` 会整份重写的文件,任何手工改动都等于**临时**改动。
> 要长期存在,就得写进生成器。

**而且**:生成完要 `grep` 一遍关键入口是否还在,不在就往日志里写醒目告警。
这次证明了「我只修了被报告的那一个」—— 报告的人只会发现他自己用的那个。

我们的记忆里本来就有一条同形状的:「给插件加新配置段必须同步改 `mc-ai-regen` 三处,
否则后台一保存就被抹掉」。**这是同一个坑的第二次,只是换了个生成器。**

---

### ✅ 小麦的地盘已经圈上了(服务器侧逐格复核过)

`2026-09-17 13:47:24` —— 服务器原话 `Claim created! Use /trust to share it with friends.`

**不只信机器人自己的报告**,从服务端复核了一遍:

| 复核项 | 结果 |
|---|---|
| `ClaimData/*.yml` 文件数 | 21 → **22** |
| 新增 `116.yml` 的 Owner | 小麦自己的 UUID |
| 范围 | x −26~−16 / z 156~166 = **121 格** |
| 房子西北角 / 东南角 / 新箱子 / 家与重生点 | **4/4 都落在它自己的领地里** |
| `land.json` | `{"done":true,...}` → 不会再重复圈 |

走的正是设计好的两步:第一次按"资产包围盒 + 两边各留 2 格"算 → GP 回
`Your selected area overlaps an existing claim.` → 置 `hugWest`
→ 第二次**把东边余量整个去掉**,让东边界正好贴在最东那件资产上 → 一次就成。
(这条路之前因为 `const x2` 那个 `TypeError` 从来没跑起来过。)

**仍然没解决的**:**老箱子** `(-15,63,159)` 还锁在隔壁的领地里 ——
而且那块地当天又扩大了(z 156~219 → **148~219**,2232 → 2664 格)。
圈地保住的是**新**箱子和房子,老箱子里那十几组木头要么请对方 `/trust`,
要么由管理员处理。

---

### 圈地功能真跑起来了:真因是金铲子,以及一条我自己写过却没查的文档

**上一条记的是「已部署、还没验到」。这条记的是验到之后发现的事。**

#### 🔑 真因:`/claim` 也要手持金铲子

第一次真跑,服务器**确实收到了** `/claim 5`(Paper 日志
`XiaoMai issued server command: /claim 5`),领地文件数**一个没变**,
而机器人自己报的是「没等到服务器的回执(期间收到:一条都没有)」。

修好「认不出的回执照原话汇报」之后,一句日志就给出了答案:

```
服务器原话:「You must be holding a golden shovel to do that.」
```

**我先前判断错了**:我从控制台问出了用法串 `/claim [optional radius]`,
就据此写下「所以不需要金铲子」—— 我证明的是「这个命令存在」,
用的却是「这个命令不用铲子」。证明了弱命题,当成强命题使。

> **2026-09-18 修正(结论收窄了一点,而且更有力了)**:
> 平级会话读 GP 16.18.5 的源码,说 `/claim` 的 `onCommand` 段里没有金铲子检查。
> 两边合起来才是完整答案 —— `messages.yml` 里有**两条不同的消息**:
> ```
> 552  MustHoldModificationToolForThat:  You must be holding a golden shovel to do that.
> 564  RadiusRequiresGoldenShovel:       You must be holding a golden shovel when specifying a radius.
> ```
> **存在一条专门为「指定半径时需要金铲子」而写的消息**,这独立佐证了
> **`/claim <半径>` 和金铲子是绑在一起的**;而**不带半径**的 `/claim` 不需要铲子,
> 它的半径直接取 `AutomaticNewPlayerClaimsRadius`(本服=4,即 9×9=81 格)。
> 所以准确的说法是:**要指定半径就得手持金铲子**,不是"圈地一律要铲子"。
> ⚠️ 我们实际收到的是 552 那条通用消息、不是 564 那条专用消息,
> 所以「`/claim <半径>` 具体从哪一行抛出」这件事**还没查到源码级**。
> 但现场证据是一次干净的前后对照:同一位置、同一条 `/claim 5`,
> 无铲子 4 次全是铲子提示,拿到铲子后立刻收到真正的圈地回执(先重叠、后 `Claim created!`)。

**更该记一笔的是**:`03-land-claims.md` 里**我自己当天上午就写了**
「② 在菜单「领地」里免费领金铲子 ③ 手持金铲子,左键点地盘的两个对角」。
答案就在我几个小时前推进这个仓库的文件里,实现的时候我没去查。
Owner 让建这个目录的原话是「以后好查询 不然你很快就会失忆的」——
第一次能派上用场的机会,我自己没用。**写文档和查文档是两件事。**

金铲子在这台服务器上是**权限凭证,不是要自己打的家具**:
`land.yml` 里有个按钮「领取金铲子 / Free golden shovel」,
动作就是 `[console] give %player_name% golden_shovel 1`。
小麦点不了菜单,所以按同一个方式给了它一把(可逆:`clear XiaoMai golden_shovel`)。

#### 另外三个一起修掉的

| 问题 | 症状 | 改法 |
|---|---|---|
| `const x2` 后面又赋值 | hugWest 重试路径一触发就抛 `TypeError`,被 catch 吞掉返回 null —— **那条「撞重叠后一步挪到位」的路从来没跑起来过** | 改成 `let` |
| 回执被别的分支吃掉 | GP 的 `NoBuildPermission` 和「别人的地不许挖」是**同一句话**,而那个分支里有 `return` | 收集器提到最前面,并且**收全部消息**,认不出的照原话汇报 |
| 播报的范围不是真要圈的范围 | 日志写「站在 (-16,161) 打 /claim 5 —— 打算圈 x -22~-12」,而 −16±5 其实是 x −21~−11(`GoalNearXZ` 留了 1 格容差,我却拿计划中心去播报) | 按**脚下那一格**重算,逐件资产验一遍,漏了就**不发命令**(GP 不让把领地改小,圈错比不圈更麻烦) |

#### 分类器补齐的原话

原来漏了这几条,收到了也当噪音丢掉:
`NoCreateClaimPermission`(没有圈地权限)、
`NoChatUntilMove`(GP 的防机器人门:「你得再走两步才能说话」——
传送回家不算"走过路",很容易撞上,现在会当场走两步重发一次)、
`ClaimCreationFailedOverClaimCountLimit`(领地个数到上限)、
以及金铲子那条。

**这个目录的规矩**:不能把「我认不出」写成「服务器没回」。

---

### AI 玩家小麦:治「做事做一半就跑」

**Owner 2026-09-17 原话**:「小麦智商看起来还是很低下 做事情做一半就不做了
干别的去了 这样永远干不好」。

查了 09:00~13:11(UTC)的 **1427 次决策** —— 他说得对,但真因不是"笨":

| 量出来的 | 值 |
|---|---|
| 决策 1427 次,真正开始执行 | **1049(26% 的决策根本没落地)** |
| 离家距离 中位 / p90 / 最远 | **76 / 165 / 259 格** |
| 待在离家 60 格以外的时刻占比 | **64%** |
| `gather_wood` 被选中 | **517 次 = 落地决策的 36%**(另有 159 次被挡回;全部回答里想砍树约 42%) |
| 相邻两次 `explore` 同向 | 44%(乱猜是 25%) |

决定性的一条:13:11 它在 `(-16,318)`,地图写着「能砍的林子=**北边** 131 格」——
那片林子在 z≈187,**离家只有 29 格**。
**林子一直在家门口,是它自己往南飘了 160 格**,然后一次次报「附近没树」。
而它的家当全在家:箱子(2002 件)、工作台、床、刚盖好的房子。

它不是不会做事,是**永远在路上**。

**两条硬规则**(为什么必须是硬规则:`explore` 一次只走 12 格,
而它根本没有"我正往哪儿去"这个概念 —— 本目录已六次验证「前置条件靠叮嘱治不好」):

- **回家绳**:离家超过 100 格 → 直接 `/home base` 传送回家(传送不吃走路预算,
  而实测它的真实位移速度中位只有 0.86 格/秒,走 100 格要两分钟)。60 秒一次。
- **缺木头就去林子**:身上木头 <20、附近确实没有能砍的树、但记得林子在哪
  → 强制 `goto_place 林子`。到了会**自己接着砍**(一个动作把"走过去 + 砍掉"
  两件事一次办完,中间不再插一次决策)—— 这正是"做一半就跑"的直接对策。

#### ⚠️ 这条规则差点变成我自己造的死循环

部署当场就撞上:它记着的那片林子 `(-29,178)` **落在别人的领地里**,
服务器原话「那一带是别人的领地,服务器不让我动」。
而 `places.js` 这个地图模块原来**只会记住、从不划掉** ——
那条新规则会每 45 秒把它送回同一片砍不动的林子。

所以补了 `places.strike()`:同一个地方连着两次干不成事就**划掉,不再推荐**;
成功一次就把污点清零(别人撤了领地还能用回来)。
两次而不是一次,因为单次失败常常只是寻路超时。

**验到的**:改完 5 分钟内它就记下了一片新的、离家 ~20 格、
`砍到3块` 的林子,位置全程留在家附近。

**没验到的**:回家绳本身还没触发过(改完之后它一直没飘出 100 格),
所以那条只能算"写好了、逻辑走通了",不算"验到了"。按这个目录的规矩写清。

---

### AI 玩家小麦:会自己给地盘圈领地了

**为什么**:它的东西**一格保护都没有**,而且已经因此吃过一次亏。

实测查出来的处境(都是当天从服务器读的):
- 它名下**已经有**一块领地 `ClaimData/113.yml`(x −2~6, z 135~143, 81 格)。
  那是 GriefPrevention 的 `AutomaticNewPlayerClaimsRadius: 4` 在它
  **第一次放下箱子的同一秒**自动圈的 —— 它自己不知道,而且后来搬家了,
  所以那块地现在保护的是一片空地。
- 现在的房子 `(-19,63,160)` 4×4、新箱子 `(-18,64,160)`、重生点 `(-17,158)`
  **都在任何领地之外**(21 个 claim 文件逐个算过边界)。
- 而隔壁孩子当天 10:27 UTC 圈了 `x -15~21 / z 156~219`,它的**老箱子** `(-15,63,159)`
  正好落在那块地最西边那一列上 → 右键被 GP 取消、`windowOpen` 不回包 →
  连着 5 次「打不开箱子」,里面十几组木头至今锁着。
- 自动圈地**只对名下零领地的玩家触发**,113 还在,就不会有第二次。

**🔑 实现上最关键的发现**:这台服务器的 GP **支持命令式圈地**。
直接问服务器,它回的是用法串 `/claim [optional radius]`。
所以**不需要**「领金铲子 → 走到角 1 右键 → 走到角 2 右键」那一套
(而且 `land.yml` 那个菜单根本**没有 `open_command`**,金铲子那条路反而更难走)。
一条命令,少了四个会失败的环节。

**改哪儿**:`~/mcbot/bot.real.js` + `~/mcbot/roles.json`
(动作清单 16 → 17 个,多了 `claim_land`。**`roles.json` 是执行闸不是菜单** ——
不加进去整套逻辑就是死码,这条当天已经吃过一次亏)。

**怎么做的**:
- 范围**在运行时从它自己的 `house.json` / `mychest.json` / `home.json` 算**,不写死坐标 ——
  Owner 的指令是「让他自己给自己的地盘圈领地」。
- 半径下限 5:GP 的门槛是 `MinimumArea: 100` + `MinimumWidth: 5`,而 `/claim r` 出来的是
  (2r+1)²,所以 r=5 → 11×11 = 121 格刚好过线。它有 3772 格可用。
- 离目标 >24 格就先 `/home base` 传送 —— 走路预算 15 秒,而实测速度中位只有
  0.86 格/秒(全天 5878 个位移样本),15 秒撑死 36 格,而它经常在 50~150 格外游荡。
- **成败只认 GP 的回执原文**,分类正则逐字来自 `GriefPreventionData/messages.yml`。
  **不看「命令发出去没报错」** —— 那正是同一天在资源世界菜单上栽过的坑
  (「配置加载成功」不等于「玩家看得见」)。
- 撞重叠就**一步挪到位**:把东边余量整个去掉,让领地东边界正好贴在最东那件资产上。
  用真实数据模拟过两轮:第一次 站 (−18,160) `/claim 5` → x −23~−13 **必然重叠**;
  第二次 站 (−21,160) `/claim 5` → x −26~−16,**盖住全部资产、零重叠、121 格** ✅。
  (原来写的「每次往西挪 2 格」要三次、十几分钟才碰对。)

**回滚**:
```bash
# 代码:换回上一版 d8eb936e832a
# 动作清单:
sudo cp ~/mcbot/roles.json.bak-claim-20260917-214106 ~/mcbot/roles.json
sudo systemctl restart mcbot
```

**⚠️ 还没验到的**:部署时是夜里、它血只有 6、离家 58 格 ——
硬规则的三道门槛(白天 / 血>14 / 5 分钟节流)**全部按设计拦住了**,所以还没真跑过一次。
`land.json` 还不存在就是证据。天亮之后才能看到结果。

---

### AI 玩家小麦:加了"做床 + 天黑睡觉"

**为什么**:实测它的死亡有 **61% 发生在重生点 8 格内**(样本 33),而重生点由服务端插件决定
(EssentialsX `respawn-at-home: true`),客户端写任何"别回家"的规则都管不了。
睡觉能从根上拿掉"夜晚"这个变量,而且 `respawn-at-home-bed: true` 让重生点跟着床走。

**改哪儿**:`~/mcbot/bot.real.js` + `~/mcbot/roles.json`(给它的动作清单加了 `sleep`)。
**注意 `roles.json` 是执行闸不是菜单** —— 不加进去的话整套睡觉逻辑是死码。

**效果**:2026-09-17 20:26 JST 第一次真睡着,夜被跳过。它睡的是一张**村庄自然生成的床**
(CoreProtect 里全服没有任何玩家放过床的记录),不是孩子的。

**回滚**:环境变量 `SLEEP_AT_DUSK=0` 一键关掉这套行为,不用改代码;
`roles.json` 的备份是 `roles.json.bak-sleep-20260917-123914`。

---

## 更早的改动(来自助手的笔记,**日期未逐条复核**)

⚠️ 下面这些是从助手的记忆整理的,不是当场从服务器验的 —— 引用时请自己确认一次。

| 时间 | 改动 | 要点 |
|---|---|---|
| 2026-07-19 | 认证换成 nLogin 2.0.18 | 替掉 LibreLogin(它和 Via/Floodgate 一起会静默断连);**不要再装回 LibreLogin** |
| 2026-07-02 | 移除 WorldEdit | Owner 要求(和指南针导航冲突);若重装要锁 ≤7.4.2 并重修指南针 |
| — | EssentialsX 固定 2.21.0 | **不要升到 2.22.0+**,那之后放弃了 1.21.4 支持 |
| — | 开放基岩版 | Geyser + Floodgate,端口 19132;要同时关 Paper 的 `perform-username-validation` 并放宽 nLogin 的名字正则,否则带点号的名字进不来 |
| — | BlueMap 取消管理员密码、49 个资源世界全部上图 | **`render-thread-count` 必须 = 2**;调到 6 会吃掉 11/16 核,玩家卡在「加载地形中」而 TPS 仍显示 20.0 |
| — | FreedomChat 1.7.2 | 治「无法验证聊天消息」那个提示;必须把 `claim-secure-chat-enforced` 设成 `true`,而且要**完整重启**(没有 reload 命令) |
| — | 备份到 Google Drive | 每天 04:00 JST;曾连续 3 天**静默失败**(tar 对热的 plugins 目录返回 rc=1),已改成容忍 rc=1 |

---

## 写新条目时的模板

```markdown
### 〈一句话说清改了什么〉

**为什么**:

**改哪儿**:〈文件路径〉

**怎么做的**:〈关键配置/代码,贴出来〉

**回滚**:
```bash
〈一条命令〉
```

**⚠️ 没核实的**:〈老实列出来〉
```
