# 一份代码,两个身份

> Owner 在游戏里看到:
> ```
> Bingolds  xiaomai的领地能不能取消掉一块
> MaiNiang  这个我不清楚,你问问管理员      ← 它以为在说它
> XiaoMai   这个我不清楚,你问问管理员
> ```
> 他问的是「怎么 mainiang 也回复了」。
> **而真正的问题不是它多说了一句话。**

## 一行

```js
const NAME_RE = /小麦|xiaomai/i          // bot.real.js:186
const mentioned = NAME_RE.test(message)  // :7215
```

两个 AI 玩家跑的是**同一份代码**,只是用不同的账号登录:

```
mcbot.service    XiaoMai    EnvironmentFile=/etc/mcbot.env
mcwife.service   MaiNiang   EnvironmentFile=/etc/mcwife.env    ← 同一个 bot.js
```

登录名是按实例来的(`process.env.MC_NAME || ROLE.login`),
**但"有没有人在叫我"这个判据是写死的。** 于是第二个实例拿着第一个实例的名字在认领消息。

## 为什么这不是"多说一句话"的问题

`mentioned` 同时把着四道门:

```
:7215  回话
:7231  RE_FOLLOW && mentioned   →「小麦 跟我来」
:7237  RE_WHERE  && mentioned   →「小麦 你在哪」
:7242  RE_GOAL   && mentioned   →「小麦 进度呢」
```

也就是说:**任何孩子说一句「小麦 跟我来」,麦娘也会跟他走。**

而代码里就在那道门旁边写着(原文):

> 🔴 必须【提到名字】才算指令。两个理由:
> ① Owner 的规格;② 更要紧的 —— 原来这条【不要求名字】,于是任何孩子在公屏说一句"跟我来"
>    就能把机器人牵走。那不只是吵不吵,是【谁能指挥它】的问题。

**护栏写对了,身份判据写死了,于是护栏对第二个实例整个失效。**
加护栏的人和埋雷的人是同一个,只隔了几百行。

## 为什么它能活这么久

三个条件要同时满足才看得见:

1. **两个实例同时在线** —— 麦娘是当天才上线的
2. **有人在公屏点名** —— 而 Owner 的规格是"不被点名就不说话",所以这种消息本来就少
3. **有人正好在看** —— 这次是 Owner 自己看见的

更根本的是**它的失效方式**:

> **不是"报错",不是"少做了一件事",是"多做了一件事"。**
> 没有异常、没有告警、没有一行日志说"我不该回这句"。
> 在日志里,麦娘那句回话和一次正常回话**长得一模一样**。

## 排查方法

### ① 找"本该由身份推导、却写成字面量"的东西

一份代码跑 N 个实例时,任何等于**实例 #1** 的字面量都是雷:

```bash
# 名字
grep -n "小麦\|xiaomai" bot.real.js
# 路径(同类:第二个进程会覆盖第一个的存档)
grep -nE "'/[a-z/]*\.(json|db|log)'" bot.real.js
# 端口、账号、ID
grep -nE ":[0-9]{4,5}'|process\.env\.[A-Z_]+ *\|\| *'" bot.real.js
```

判据不是"这个常量对不对",是:**换一个实例来跑,它还对吗?**

平级会话在同一天踩到了这一类的第二个:**存盘文件路径写死** ——
麦娘一上线,第一件事就是拿自己的进度覆盖小麦的 `house.json` / `land.json`,
而那两样是花了一整天做出来、还在服务器侧逐格复核过的。

> **私有的按实例隔开,共有的显式共享。**
> 而且:**第一个实例不设隔离变量** —— 这样它现有的存档零迁移,不用动。

### ② 让每个实例在启动时把"我是谁、我认什么"说出来

这是修复的一部分,不是装饰:

```
👤 我是【小麦】(XiaoMai,角色 son)
   有人喊这些名字我才当是在叫我:XiaoMai / 小麦(别人的名字一概不理)
👤 我是【麦娘】(MaiNiang,角色 wife)
   有人喊这些名字我才当是在叫我:MaiNiang / 麦娘(别人的名字一概不理)
```

> **这个 bug 能活到现在,是因为从来没有一行日志说过它认什么名字。**
> 只要这行早存在,任何人扫一眼日志就会发现两个进程认的是同一个名字。

**把"不变量"打印出来**,比事后去推它有没有被违反便宜得多。

### ③ 验证修复时,分清「改了」「部署了」「正在跑的就是它」

```bash
stat -c %y bot.real.js                              # 代码改于 01:45:06
systemctl show mcbot  -p ActiveEnterTimestamp       # 进程起于 01:45:17
systemctl show mcwife -p ActiveEnterTimestamp       # 进程起于 01:50:23
```

进程晚于代码 = 跑的是新版。但最硬的证据是**运行中的进程自己把修好的那件事打出来** ——
上面那两行启动日志就是。它同时证明了三件事:代码改对了、部署到位了、而且**两个实例确实各认各的名字**。

## 教训

**1. 🔴 一份代码跑多个实例时,任何写死的"自身标识"都是炸弹。**
   名字、路径、端口、账号。它对第一个实例永远是对的 ——
   **所以你测第一个实例,永远测不出来。**

**2. 失效方式是"多做了一件事"的 bug,最难被发现。**
   少做了会有人喊,报错了有日志,而"多做了一件事"在日志里和正常行为长得一样。
   这类 bug 要靠**不变量**发现,不能靠观察。

**3. 把不变量打印出来,当成功能的一部分。**
   「我认这些名字」这一行不是日志噪音,它是**检测器**。
   代价是一行,收益是"任何人扫一眼就能发现两个实例撞身份"。

**4. 一个护栏可能在几百行之外被自己废掉。**
   「必须点名才算指令」这条写得很清楚、理由也写对了 ——
   然后 186 行那个写死的常量把它对第二个实例整个关掉了。
   **护栏的强度 = 它依赖的那个判据的强度。**

> **加实例之前先问:这份代码里有多少东西,是"第一个我"的?**

---

⚠️ **附:这篇里我没复核的**

- 修复是平级会话做的(`bot.real.js` 归它)。我独立复核了:
  线上代码的 `SELF_NAMES` 推导、正则转义、启动日志那一行、两个进程的启动时间晚于代码修改、
  以及两个进程各自打出的"我认什么名字"。
  **但它说的"离线跑了三个角色 × 六句话的矩阵",我没有看过那份输出。**
- 我**没有在游戏里真的喊一句「小麦 跟我来」**去看麦娘动不动 ——
  那会在孩子面前把机器人牵走。线上证据到"两个进程认的名字不同"为止。
- "存盘路径写死"那个同类问题是平级会话自己报的,**我没有独立复核**。

版本:`bot.real.js`(2026-09-18 01:45 那版)、两个 systemd 服务 `mcbot` / `mcwife`、
角色档案 `roles.json`(`ROLE.login` / `ROLE.cnName`)。

---

## 续(2026-09-18):提示词里也写着「你是小麦」

> 这一节是另一个会话补的。**状态:可疑,有反例,因果没证明,也还没修。**

### 怎么冒出来的

2026-09-18 12:56 JST,「模型答砸了要记账」那一刀上线([27](27-the-loudest-is-not-the-costliest.md))。
以前模型答砸了都被悄悄扔掉,现在看得见了。

上线后 10 分钟里(12:57:40 → 13:07:39 JST),麦豆的单步决策失败 6 次,**6 次都是想去砍树**。
每次都被代码挡回:「动作「gather_wood」现在做不到」。

拉长到一小时(麦豆自己打的汇总行,约 12:57 → 13:57 JST):**单步决策问了 34 次,33 次答不对,只有 1 次能用。**
那 33 次失败日志里一条都没被节流压住,**33 次全是想去砍树**。
麦娘也不会砍树。她同一小时失败 12 次,看得到内容的 9 次里,6 次是做不了的合成,**3 次也是想去砍树**。

**麦豆根本不会砍树。** `roles.json` 里三个人会的动作:

```
小麦  19 个,有 gather_wood
麦娘  15 个,没有 gather_wood(她是种地的)
麦豆   6 个:eat / flee / wander / go_home / goto_place / idle
```

### 查下去看到的

问模型时,发过去的东西分三块:

1. **角色自己的人格**,放在最前面。
2. **三个人共用的一段说明**。这段是写给小麦的:

   ```
   brain.js:60   单步决策   「……一个 AI 玩家,名字叫小麦(XiaoMai)。」
   brain.js:102             「收集木头就该多选 gather_wood。」
                 可选动作的第一条就是它(:69),例子里也反复是它(:91、:132-133、:137)
   brain.js:656  聊天回话   「你叫小麦(XiaoMai)……」
                 后面还跟着小麦的性格:「干活踏实、话不多、有点憨」
   ```

3. **当时的处境**(一段 JSON)。这里面**有自己的真名**:`'我是谁': bot.username`(`bot.real.js:1135`)。
   麦豆拿到的就是 `MaiDou`。

所以模型拿到的是**互相打架的身份**:

- 共用说明说「你叫小麦」;
- 处境里写着「我是谁:MaiDou」;
- 麦娘和麦豆的人格里都没写自己叫什么。麦娘的人格还有一句「你比小麦更会打算」,说明她不是小麦。

聊天那条路也一样打架。有人点名「麦豆」,它才会回话,可说明里写的是「你叫小麦」。

**往「砍树」推的也不只是那段说明。** 麦豆的处境里还带着小麦的砍树字段:

- 「附近有树」;
- 「背包原木」;
- 「目标进度:已存进箱子 X/64 块」(`bot.real.js:1141`,`:947` 注释写着「背包里的原木数量 —— 这就是目标进度的度量」)。

### 反例:改掉名字未必就好

- **规划师的提示词早就按角色生成了**(`brain.js:396-402` 那段注释),里面给了麦豆自己的动作清单。
  它照样给麦豆派了「去林子砍树」。平级会话的结论是**「小模型不遵守」**,见 `server/CHANGELOG.md` 规划师那条。
- **小麦自己也偏爱砍树。** 2026-09-17 日本时间 18:00~22:11(日志里是 UTC 09:00~13:11),那时只有小麦一个机器人。
  我重数了一遍:被执行的 1422 次决策里 **517 次(36%)是砍树**,另有 159 次想砍树、被代码挡回。
  按「模型的所有回答里有多少想砍树」算,是 676 / 1604 ≈ **42%**。
  (`server/CHANGELOG.md`「职业 + 脚本」那条写的是「681 次 = 48%」,和我重数的对不上,已转告管 CHANGELOG 的会话。)
  所以「收集木头就该多选 gather_wood」这句对小麦也未必是好话,只是砍树至少是它会的动作。

所以现在只能说:**提示词写死小麦、推砍树,这是真的;它是不是麦豆老想砍树的原因,不知道。**
也可能根本就是小模型爱砍树。

### 为什么上面的「排查方法 ①」没扫到它

① 里那条找名字的命令有两个毛病:

```bash
grep -n "小麦\|xiaomai" bot.real.js
```

- **只扫了 `bot.real.js` 一个文件。** 身份字面量藏在 `brain.js` 里,那里有 8 行带着「小麦」。
- **分大小写。** 它搜不到「XiaoMai」这种写法。

> **排查范围 = 这个进程加载的所有代码 + 喂给模型的所有文字**,不是「出事的那个文件」。
> 提示词也是代码。里面写死的名字,和写死的存档路径一样是雷。

应该这样扫:

```bash
grep -rniE '小麦|xiaomai' ~/mcbot --include='*.js' --include='*.json' --exclude-dir=node_modules --exclude-dir=data
```

**先确认它能搜到 `brain.js:60` 那一行,再相信它搜出来的其它结果**(为什么要这样,见 [28](28-not-found-is-not-never-said.md))。

### 现在知道到哪一步

| 说法 | 核到哪一步 |
|---|---|
| 共用说明写死小麦、推 gather_wood | ✅ 读线上 `brain.js` 核过 |
| 处境里「我是谁」是 MaiDou | ✅ 读线上 `bot.real.js:1135` 核过;麦豆的启动日志也自报 MaiDou |
| 麦娘、麦豆的人格里没写自己的名字 | ✅ 读线上 `roles.json` 核过 |
| 麦豆 6 次失败全是单步决策选了 gather_wood | ✅ 逐行数日志核过(12:57:40 → 13:07:39 JST) |
| 麦豆一小时单步决策 34 次、33 次答不对,33 次全是砍树 | ✅ 它自己的「🧠📊」汇总行(约 12:57 → 13:57 JST),加逐行数日志(没有被节流压住的) |
| 这就是它老想砍树的原因 | ⏳ **没证明**。有反例(见上) |
| 聊天时自称小麦 | ⏳ 还没看到过。截至 2026-09-18 13:07 JST,麦娘在整份日志里只回过 1 句话(本篇开头那句),麦豆 0 句 |

### 判定实验(便宜,还没做)

抓几份麦豆的真实处境(「附近有树」是和否的都要有,它两种情况下都选过砍树)。
每份处境配下面几套提示,**每套至少问 20 次**,数「选了不会的动作」各几次:

| 套 | 改了什么 |
|---|---|
| A | 什么都不改,就是现在的样子 |
| B | **只把名字**换成麦豆,别的不动 |
| C | 名字换成麦豆,**共用说明里的规则和例子也只留它会的动作** |
| D | 在 C 的基础上,**再把处境里那几个砍树字段拿掉**(附近有树、背包原木、目标进度 X/64 块) |

- A 和 B 差得多,说明是名字的事。
- B 和 C 差得多,说明是规则和例子的事。
- C 和 D 差得多,说明是处境里那几个字段的事。
- 四套都差不多,才能说是小模型自己爱砍树,改提示词没用。

**动手之前先定好「差多少才算差得多」**,别看了结果再定。
现在的底数是 34 次里错 33 次。

**问哪台机器、哪个模型,要固定住并记下来。**
线上走的是模型网关,网关会在几台机器、两种模型之间轮换。四套如果落在不同的机器上,比出来的差别就说不清是谁造成的。
**别在 Owner 正在用的那台笔记本上跑。**

**排期**:等「遇事才想」那一刀上线、核完之后,单独一刀。一次只改一样,出了问题才分得清是哪一刀。

> 名字这一半,正是上面教训 1 说的:「它对第一个实例永远是对的 —— 所以你测第一个实例,永远测不出来。」
> 小麦读「你叫小麦」,一点毛病都没有。
